/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.resources.costs;

import com.paid.api.core.ClientOptions;
import com.paid.api.core.RequestOptions;
import com.paid.api.resources.costs.requests.CostIngestRequest;
import com.paid.api.types.CostIngestResponse;

public class CostsClient {
    protected final ClientOptions clientOptions;

    private final RawCostsClient rawClient;

    public CostsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawCostsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawCostsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Ingests a batch of cost records. Each record is either a pre-computed <code>cost</code> (caller supplies amount + currency) or a <code>usage</code> record (caller supplies vendor/model/token counts and Paid prices it server-side). The batch is all-or-nothing: if any record fails validation, the entire request is rejected with a 400 and nothing is persisted.
     */
    public CostIngestResponse createCosts(CostIngestRequest request) {
        return this.rawClient.createCosts(request).body();
    }

    /**
     * Ingests a batch of cost records. Each record is either a pre-computed <code>cost</code> (caller supplies amount + currency) or a <code>usage</code> record (caller supplies vendor/model/token counts and Paid prices it server-side). The batch is all-or-nothing: if any record fails validation, the entire request is rejected with a 400 and nothing is persisted.
     */
    public CostIngestResponse createCosts(CostIngestRequest request, RequestOptions requestOptions) {
        return this.rawClient.createCosts(request, requestOptions).body();
    }
}
