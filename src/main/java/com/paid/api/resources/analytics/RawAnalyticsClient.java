/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.resources.analytics;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.paid.api.core.ClientOptions;
import com.paid.api.core.MediaTypes;
import com.paid.api.core.ObjectMappers;
import com.paid.api.core.PaidApiApiException;
import com.paid.api.core.PaidApiException;
import com.paid.api.core.PaidApiHttpResponse;
import com.paid.api.core.QueryStringMapper;
import com.paid.api.core.RequestOptions;
import com.paid.api.errors.BadRequestError;
import com.paid.api.errors.ForbiddenError;
import com.paid.api.errors.InternalServerError;
import com.paid.api.errors.RequestTimeoutError;
import com.paid.api.errors.TooManyRequestsError;
import com.paid.api.resources.analytics.requests.GetSignalsMetadataRequest;
import com.paid.api.types.AnalyticsQueryRequest;
import com.paid.api.types.AnalyticsQueryResponse;
import com.paid.api.types.AnalyticsSchemaResponse;
import com.paid.api.types.ErrorResponse;
import com.paid.api.types.SignalsMetadataResponse;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawAnalyticsClient {
    protected final ClientOptions clientOptions;

    public RawAnalyticsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Runs a single ClickHouse SELECT (or WITH … SELECT) against your organization's analytics views. Before writing a query, call <code>getAnalyticsSchema</code> (GET /schema) for the available views and columns, and <code>getSignalsMetadata</code> (GET /signals/metadata) for the JSON paths inside <code>fact_signal.data</code>. Results are automatically scoped to your organization — no org filter is needed or possible. Only SELECT/WITH statements are accepted.
     * <p>Conventions: monetary amounts are minor units (cents — divide by 100 for the major unit); most are integers, but <code>fact_cost.cost_amount</code> is fractional cents (Decimal) since a single AI call usually costs less than a cent; 64-bit integers (counts, ids, amounts) are returned as JSON strings to preserve precision, so parse them client-side; Decimal columns (fractional cents, and credit amounts, which are counts of credits rather than cents and are never divided by 100) come back as JSON numbers instead, so a value beyond 2^53 is already rounded — select toString(col) when you need its exact digits. Query signal payloads via JSON paths, e.g. <code>SELECT data.country::String AS country, count() FROM fact_signal GROUP BY country</code>.</p>
     * <p>Limits: 30 seconds of execution time and 10,000 result rows (truncation is flagged via <code>meta.truncated</code>). Prefer aggregates and a <code>created_at</code> date filter on large tables — this endpoint is for interactive analytics, not bulk export.</p>
     */
    public PaidApiHttpResponse<AnalyticsQueryResponse> executeAnalyticsQuery(AnalyticsQueryRequest request) {
        return executeAnalyticsQuery(request, null);
    }

    /**
     * Runs a single ClickHouse SELECT (or WITH … SELECT) against your organization's analytics views. Before writing a query, call <code>getAnalyticsSchema</code> (GET /schema) for the available views and columns, and <code>getSignalsMetadata</code> (GET /signals/metadata) for the JSON paths inside <code>fact_signal.data</code>. Results are automatically scoped to your organization — no org filter is needed or possible. Only SELECT/WITH statements are accepted.
     * <p>Conventions: monetary amounts are minor units (cents — divide by 100 for the major unit); most are integers, but <code>fact_cost.cost_amount</code> is fractional cents (Decimal) since a single AI call usually costs less than a cent; 64-bit integers (counts, ids, amounts) are returned as JSON strings to preserve precision, so parse them client-side; Decimal columns (fractional cents, and credit amounts, which are counts of credits rather than cents and are never divided by 100) come back as JSON numbers instead, so a value beyond 2^53 is already rounded — select toString(col) when you need its exact digits. Query signal payloads via JSON paths, e.g. <code>SELECT data.country::String AS country, count() FROM fact_signal GROUP BY country</code>.</p>
     * <p>Limits: 30 seconds of execution time and 10,000 result rows (truncation is flagged via <code>meta.truncated</code>). Prefer aggregates and a <code>created_at</code> date filter on large tables — this endpoint is for interactive analytics, not bulk export.</p>
     */
    public PaidApiHttpResponse<AnalyticsQueryResponse> executeAnalyticsQuery(
            AnalyticsQueryRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("analytics/query")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PaidApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new PaidApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), AnalyticsQueryResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 408:
                        throw new RequestTimeoutError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            throw new PaidApiApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new PaidApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns the analytics views available to POST /query, with column names, ClickHouse types, and descriptions. Dimensions (<code>dim_*</code>) describe entities; facts (<code>fact_*</code>) are event/transaction tables that join to dimensions via the <code>*_id</code> columns described in each comment.
     */
    public PaidApiHttpResponse<AnalyticsSchemaResponse> getAnalyticsSchema() {
        return getAnalyticsSchema(null);
    }

    /**
     * Returns the analytics views available to POST /query, with column names, ClickHouse types, and descriptions. Dimensions (<code>dim_*</code>) describe entities; facts (<code>fact_*</code>) are event/transaction tables that join to dimensions via the <code>*_id</code> columns described in each comment.
     */
    public PaidApiHttpResponse<AnalyticsSchemaResponse> getAnalyticsSchema(RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("analytics/schema")
                .build();
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new PaidApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), AnalyticsSchemaResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                if (response.code() == 500) {
                    throw new InternalServerError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            throw new PaidApiApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new PaidApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Lists the JSON paths (and their observed types) present in the <code>data</code> payload of your signals within a time window (default: last 30 days), grouped by signal name. Use the returned paths in queries against <code>fact_signal</code>, e.g. <code>WHERE data.&lt;path&gt;::String = '...'</code>.
     */
    public PaidApiHttpResponse<SignalsMetadataResponse> getSignalsMetadata() {
        return getSignalsMetadata(GetSignalsMetadataRequest.builder().build());
    }

    /**
     * Lists the JSON paths (and their observed types) present in the <code>data</code> payload of your signals within a time window (default: last 30 days), grouped by signal name. Use the returned paths in queries against <code>fact_signal</code>, e.g. <code>WHERE data.&lt;path&gt;::String = '...'</code>.
     */
    public PaidApiHttpResponse<SignalsMetadataResponse> getSignalsMetadata(GetSignalsMetadataRequest request) {
        return getSignalsMetadata(request, null);
    }

    /**
     * Lists the JSON paths (and their observed types) present in the <code>data</code> payload of your signals within a time window (default: last 30 days), grouped by signal name. Use the returned paths in queries against <code>fact_signal</code>, e.g. <code>WHERE data.&lt;path&gt;::String = '...'</code>.
     */
    public PaidApiHttpResponse<SignalsMetadataResponse> getSignalsMetadata(
            GetSignalsMetadataRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("analytics/signals/metadata");
        if (request.getSignalName().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "signalName", request.getSignalName().get(), false);
        }
        if (request.getFromDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "fromDate", request.getFromDate().get(), false);
        }
        if (request.getToDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "toDate", request.getToDate().get(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new PaidApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), SignalsMetadataResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            throw new PaidApiApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new PaidApiException("Network error executing HTTP request", e);
        }
    }
}
