/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.resources.credits.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.ObjectMappers;
import com.paid.api.resources.credits.types.GetCreditTransactionSummaryRequestGroupBy;
import com.paid.api.resources.credits.types.GetCreditTransactionSummaryRequestInterval;
import com.paid.api.resources.credits.types.GetCreditTransactionSummaryRequestType;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = GetCreditTransactionSummaryRequest.Builder.class)
public final class GetCreditTransactionSummaryRequest {
    private final Optional<String> customerId;

    private final Optional<String> externalCustomerId;

    private final Optional<String> creditsCurrencyId;

    private final Optional<String> creditCurrencyKey;

    private final Optional<GetCreditTransactionSummaryRequestType> type;

    private final Optional<String> orderId;

    private final Optional<String> eventName;

    private final String createdAtFrom;

    private final Optional<String> createdAtTo;

    private final GetCreditTransactionSummaryRequestGroupBy groupBy;

    private final GetCreditTransactionSummaryRequestInterval interval;

    private final Map<String, Object> additionalProperties;

    private GetCreditTransactionSummaryRequest(
            Optional<String> customerId,
            Optional<String> externalCustomerId,
            Optional<String> creditsCurrencyId,
            Optional<String> creditCurrencyKey,
            Optional<GetCreditTransactionSummaryRequestType> type,
            Optional<String> orderId,
            Optional<String> eventName,
            String createdAtFrom,
            Optional<String> createdAtTo,
            GetCreditTransactionSummaryRequestGroupBy groupBy,
            GetCreditTransactionSummaryRequestInterval interval,
            Map<String, Object> additionalProperties) {
        this.customerId = customerId;
        this.externalCustomerId = externalCustomerId;
        this.creditsCurrencyId = creditsCurrencyId;
        this.creditCurrencyKey = creditCurrencyKey;
        this.type = type;
        this.orderId = orderId;
        this.eventName = eventName;
        this.createdAtFrom = createdAtFrom;
        this.createdAtTo = createdAtTo;
        this.groupBy = groupBy;
        this.interval = interval;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Filter by customer ID.
     */
    @JsonProperty("customerId")
    public Optional<String> getCustomerId() {
        return customerId;
    }

    /**
     * @return Filter by customer external ID.
     */
    @JsonProperty("externalCustomerId")
    public Optional<String> getExternalCustomerId() {
        return externalCustomerId;
    }

    /**
     * @return Filter by credit currency ID.
     */
    @JsonProperty("creditsCurrencyId")
    public Optional<String> getCreditsCurrencyId() {
        return creditsCurrencyId;
    }

    /**
     * @return Filter by the stable machine-readable key of the credit currency.
     */
    @JsonProperty("creditCurrencyKey")
    public Optional<String> getCreditCurrencyKey() {
        return creditCurrencyKey;
    }

    /**
     * @return Ledger type to summarize. Each call summarizes exactly one type, so a spend chart never nets grants against spends. Defaults to <code>spend</code>.
     */
    @JsonProperty("type")
    public Optional<GetCreditTransactionSummaryRequestType> getType() {
        return type;
    }

    /**
     * @return Filter by the order this transaction is linked to.
     */
    @JsonProperty("orderId")
    public Optional<String> getOrderId() {
        return orderId;
    }

    /**
     * @return Filter by the usage event that produced the transaction (exact match, untrimmed). Transactions with no event (grants, invoice-void clawbacks, manual adjustments) never match.
     */
    @JsonProperty("eventName")
    public Optional<String> getEventName() {
        return eventName;
    }

    /**
     * @return Start of the window (inclusive). Required: the window may span at most 92 days. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.
     */
    @JsonProperty("createdAtFrom")
    public String getCreatedAtFrom() {
        return createdAtFrom;
    }

    /**
     * @return End of the window (inclusive). Defaults to now. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.
     */
    @JsonProperty("createdAtTo")
    public Optional<String> getCreatedAtTo() {
        return createdAtTo;
    }

    /**
     * @return Dimension to group buckets by. <code>eventName</code> groups signal-driven spends by the usage event that produced them; transactions without one (grants, invoice-void clawbacks, manual adjustments) share a bucket whose <code>groupKey</code> is null.
     */
    @JsonProperty("groupBy")
    public GetCreditTransactionSummaryRequestGroupBy getGroupBy() {
        return groupBy;
    }

    /**
     * @return Period bucket size. Buckets align to UTC calendar days, ISO weeks (Monday start), or calendar months; the first and last bucket's published bounds are clipped to the requested window.
     */
    @JsonProperty("interval")
    public GetCreditTransactionSummaryRequestInterval getInterval() {
        return interval;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof GetCreditTransactionSummaryRequest
                && equalTo((GetCreditTransactionSummaryRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(GetCreditTransactionSummaryRequest other) {
        return customerId.equals(other.customerId)
                && externalCustomerId.equals(other.externalCustomerId)
                && creditsCurrencyId.equals(other.creditsCurrencyId)
                && creditCurrencyKey.equals(other.creditCurrencyKey)
                && type.equals(other.type)
                && orderId.equals(other.orderId)
                && eventName.equals(other.eventName)
                && createdAtFrom.equals(other.createdAtFrom)
                && createdAtTo.equals(other.createdAtTo)
                && groupBy.equals(other.groupBy)
                && interval.equals(other.interval);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.customerId,
                this.externalCustomerId,
                this.creditsCurrencyId,
                this.creditCurrencyKey,
                this.type,
                this.orderId,
                this.eventName,
                this.createdAtFrom,
                this.createdAtTo,
                this.groupBy,
                this.interval);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static CreatedAtFromStage builder() {
        return new Builder();
    }

    public interface CreatedAtFromStage {
        /**
         * <p>Start of the window (inclusive). Required: the window may span at most 92 days. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.</p>
         */
        GroupByStage createdAtFrom(@NotNull String createdAtFrom);

        Builder from(GetCreditTransactionSummaryRequest other);
    }

    public interface GroupByStage {
        /**
         * <p>Dimension to group buckets by. <code>eventName</code> groups signal-driven spends by the usage event that produced them; transactions without one (grants, invoice-void clawbacks, manual adjustments) share a bucket whose <code>groupKey</code> is null.</p>
         */
        IntervalStage groupBy(@NotNull GetCreditTransactionSummaryRequestGroupBy groupBy);
    }

    public interface IntervalStage {
        /**
         * <p>Period bucket size. Buckets align to UTC calendar days, ISO weeks (Monday start), or calendar months; the first and last bucket's published bounds are clipped to the requested window.</p>
         */
        _FinalStage interval(@NotNull GetCreditTransactionSummaryRequestInterval interval);
    }

    public interface _FinalStage {
        GetCreditTransactionSummaryRequest build();

        /**
         * <p>Filter by customer ID.</p>
         */
        _FinalStage customerId(Optional<String> customerId);

        _FinalStage customerId(String customerId);

        /**
         * <p>Filter by customer external ID.</p>
         */
        _FinalStage externalCustomerId(Optional<String> externalCustomerId);

        _FinalStage externalCustomerId(String externalCustomerId);

        /**
         * <p>Filter by credit currency ID.</p>
         */
        _FinalStage creditsCurrencyId(Optional<String> creditsCurrencyId);

        _FinalStage creditsCurrencyId(String creditsCurrencyId);

        /**
         * <p>Filter by the stable machine-readable key of the credit currency.</p>
         */
        _FinalStage creditCurrencyKey(Optional<String> creditCurrencyKey);

        _FinalStage creditCurrencyKey(String creditCurrencyKey);

        /**
         * <p>Ledger type to summarize. Each call summarizes exactly one type, so a spend chart never nets grants against spends. Defaults to <code>spend</code>.</p>
         */
        _FinalStage type(Optional<GetCreditTransactionSummaryRequestType> type);

        _FinalStage type(GetCreditTransactionSummaryRequestType type);

        /**
         * <p>Filter by the order this transaction is linked to.</p>
         */
        _FinalStage orderId(Optional<String> orderId);

        _FinalStage orderId(String orderId);

        /**
         * <p>Filter by the usage event that produced the transaction (exact match, untrimmed). Transactions with no event (grants, invoice-void clawbacks, manual adjustments) never match.</p>
         */
        _FinalStage eventName(Optional<String> eventName);

        _FinalStage eventName(String eventName);

        /**
         * <p>End of the window (inclusive). Defaults to now. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.</p>
         */
        _FinalStage createdAtTo(Optional<String> createdAtTo);

        _FinalStage createdAtTo(String createdAtTo);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements CreatedAtFromStage, GroupByStage, IntervalStage, _FinalStage {
        private String createdAtFrom;

        private GetCreditTransactionSummaryRequestGroupBy groupBy;

        private GetCreditTransactionSummaryRequestInterval interval;

        private Optional<String> createdAtTo = Optional.empty();

        private Optional<String> eventName = Optional.empty();

        private Optional<String> orderId = Optional.empty();

        private Optional<GetCreditTransactionSummaryRequestType> type = Optional.empty();

        private Optional<String> creditCurrencyKey = Optional.empty();

        private Optional<String> creditsCurrencyId = Optional.empty();

        private Optional<String> externalCustomerId = Optional.empty();

        private Optional<String> customerId = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(GetCreditTransactionSummaryRequest other) {
            customerId(other.getCustomerId());
            externalCustomerId(other.getExternalCustomerId());
            creditsCurrencyId(other.getCreditsCurrencyId());
            creditCurrencyKey(other.getCreditCurrencyKey());
            type(other.getType());
            orderId(other.getOrderId());
            eventName(other.getEventName());
            createdAtFrom(other.getCreatedAtFrom());
            createdAtTo(other.getCreatedAtTo());
            groupBy(other.getGroupBy());
            interval(other.getInterval());
            return this;
        }

        /**
         * <p>Start of the window (inclusive). Required: the window may span at most 92 days. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.</p>
         * <p>Start of the window (inclusive). Required: the window may span at most 92 days. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("createdAtFrom")
        public GroupByStage createdAtFrom(@NotNull String createdAtFrom) {
            this.createdAtFrom = Objects.requireNonNull(createdAtFrom, "createdAtFrom must not be null");
            return this;
        }

        /**
         * <p>Dimension to group buckets by. <code>eventName</code> groups signal-driven spends by the usage event that produced them; transactions without one (grants, invoice-void clawbacks, manual adjustments) share a bucket whose <code>groupKey</code> is null.</p>
         * <p>Dimension to group buckets by. <code>eventName</code> groups signal-driven spends by the usage event that produced them; transactions without one (grants, invoice-void clawbacks, manual adjustments) share a bucket whose <code>groupKey</code> is null.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("groupBy")
        public IntervalStage groupBy(@NotNull GetCreditTransactionSummaryRequestGroupBy groupBy) {
            this.groupBy = Objects.requireNonNull(groupBy, "groupBy must not be null");
            return this;
        }

        /**
         * <p>Period bucket size. Buckets align to UTC calendar days, ISO weeks (Monday start), or calendar months; the first and last bucket's published bounds are clipped to the requested window.</p>
         * <p>Period bucket size. Buckets align to UTC calendar days, ISO weeks (Monday start), or calendar months; the first and last bucket's published bounds are clipped to the requested window.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("interval")
        public _FinalStage interval(@NotNull GetCreditTransactionSummaryRequestInterval interval) {
            this.interval = Objects.requireNonNull(interval, "interval must not be null");
            return this;
        }

        /**
         * <p>End of the window (inclusive). Defaults to now. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage createdAtTo(String createdAtTo) {
            this.createdAtTo = Optional.ofNullable(createdAtTo);
            return this;
        }

        /**
         * <p>End of the window (inclusive). Defaults to now. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "createdAtTo", nulls = Nulls.SKIP)
        public _FinalStage createdAtTo(Optional<String> createdAtTo) {
            this.createdAtTo = createdAtTo;
            return this;
        }

        /**
         * <p>Filter by the usage event that produced the transaction (exact match, untrimmed). Transactions with no event (grants, invoice-void clawbacks, manual adjustments) never match.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage eventName(String eventName) {
            this.eventName = Optional.ofNullable(eventName);
            return this;
        }

        /**
         * <p>Filter by the usage event that produced the transaction (exact match, untrimmed). Transactions with no event (grants, invoice-void clawbacks, manual adjustments) never match.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "eventName", nulls = Nulls.SKIP)
        public _FinalStage eventName(Optional<String> eventName) {
            this.eventName = eventName;
            return this;
        }

        /**
         * <p>Filter by the order this transaction is linked to.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage orderId(String orderId) {
            this.orderId = Optional.ofNullable(orderId);
            return this;
        }

        /**
         * <p>Filter by the order this transaction is linked to.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "orderId", nulls = Nulls.SKIP)
        public _FinalStage orderId(Optional<String> orderId) {
            this.orderId = orderId;
            return this;
        }

        /**
         * <p>Ledger type to summarize. Each call summarizes exactly one type, so a spend chart never nets grants against spends. Defaults to <code>spend</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage type(GetCreditTransactionSummaryRequestType type) {
            this.type = Optional.ofNullable(type);
            return this;
        }

        /**
         * <p>Ledger type to summarize. Each call summarizes exactly one type, so a spend chart never nets grants against spends. Defaults to <code>spend</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "type", nulls = Nulls.SKIP)
        public _FinalStage type(Optional<GetCreditTransactionSummaryRequestType> type) {
            this.type = type;
            return this;
        }

        /**
         * <p>Filter by the stable machine-readable key of the credit currency.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage creditCurrencyKey(String creditCurrencyKey) {
            this.creditCurrencyKey = Optional.ofNullable(creditCurrencyKey);
            return this;
        }

        /**
         * <p>Filter by the stable machine-readable key of the credit currency.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "creditCurrencyKey", nulls = Nulls.SKIP)
        public _FinalStage creditCurrencyKey(Optional<String> creditCurrencyKey) {
            this.creditCurrencyKey = creditCurrencyKey;
            return this;
        }

        /**
         * <p>Filter by credit currency ID.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage creditsCurrencyId(String creditsCurrencyId) {
            this.creditsCurrencyId = Optional.ofNullable(creditsCurrencyId);
            return this;
        }

        /**
         * <p>Filter by credit currency ID.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "creditsCurrencyId", nulls = Nulls.SKIP)
        public _FinalStage creditsCurrencyId(Optional<String> creditsCurrencyId) {
            this.creditsCurrencyId = creditsCurrencyId;
            return this;
        }

        /**
         * <p>Filter by customer external ID.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage externalCustomerId(String externalCustomerId) {
            this.externalCustomerId = Optional.ofNullable(externalCustomerId);
            return this;
        }

        /**
         * <p>Filter by customer external ID.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "externalCustomerId", nulls = Nulls.SKIP)
        public _FinalStage externalCustomerId(Optional<String> externalCustomerId) {
            this.externalCustomerId = externalCustomerId;
            return this;
        }

        /**
         * <p>Filter by customer ID.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage customerId(String customerId) {
            this.customerId = Optional.ofNullable(customerId);
            return this;
        }

        /**
         * <p>Filter by customer ID.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "customerId", nulls = Nulls.SKIP)
        public _FinalStage customerId(Optional<String> customerId) {
            this.customerId = customerId;
            return this;
        }

        @java.lang.Override
        public GetCreditTransactionSummaryRequest build() {
            return new GetCreditTransactionSummaryRequest(
                    customerId,
                    externalCustomerId,
                    creditsCurrencyId,
                    creditCurrencyKey,
                    type,
                    orderId,
                    eventName,
                    createdAtFrom,
                    createdAtTo,
                    groupBy,
                    interval,
                    additionalProperties);
        }
    }
}
