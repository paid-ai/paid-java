/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.resources.valuemetrics;

import com.paid.api.core.ClientOptions;
import com.paid.api.core.RequestOptions;
import com.paid.api.resources.valuemetrics.requests.ArchiveValueMetricRequest;
import com.paid.api.resources.valuemetrics.requests.CreateValueMetricRequest;
import com.paid.api.resources.valuemetrics.requests.GetValueMetricRequest;
import com.paid.api.resources.valuemetrics.requests.ListValueMetricsRequest;
import com.paid.api.resources.valuemetrics.requests.UpdateValueMetricRequest;
import com.paid.api.types.ValueMetricDetail;
import com.paid.api.types.ValueMetricListResponse;
import com.paid.api.types.ValueMetricWriteAck;
import java.util.concurrent.CompletableFuture;

public class AsyncValueMetricsClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawValueMetricsClient rawClient;

    public AsyncValueMetricsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawValueMetricsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawValueMetricsClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Returns the value metrics in the current value model, without their formulas. Archived metrics are hidden unless includeArchived is true.
     */
    public CompletableFuture<ValueMetricListResponse> listValueMetrics() {
        return this.rawClient.listValueMetrics().thenApply(response -> response.body());
    }

    /**
     * Returns the value metrics in the current value model, without their formulas. Archived metrics are hidden unless includeArchived is true.
     */
    public CompletableFuture<ValueMetricListResponse> listValueMetrics(ListValueMetricsRequest request) {
        return this.rawClient.listValueMetrics(request).thenApply(response -> response.body());
    }

    /**
     * Returns the value metrics in the current value model, without their formulas. Archived metrics are hidden unless includeArchived is true.
     */
    public CompletableFuture<ValueMetricListResponse> listValueMetrics(
            ListValueMetricsRequest request, RequestOptions requestOptions) {
        return this.rawClient.listValueMetrics(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Adds one value metric — its unit, formula, signal binding and optional monetary conversion — to the value model. The signal must already exist: an event name your organization has sent, or one referenced by usage pricing on an active product. Publishes a new value model version and recalculates delivered value for historical signals.
     */
    public CompletableFuture<ValueMetricWriteAck> createValueMetric(CreateValueMetricRequest request) {
        return this.rawClient.createValueMetric(request).thenApply(response -> response.body());
    }

    /**
     * Adds one value metric — its unit, formula, signal binding and optional monetary conversion — to the value model. The signal must already exist: an event name your organization has sent, or one referenced by usage pricing on an active product. Publishes a new value model version and recalculates delivered value for historical signals.
     */
    public CompletableFuture<ValueMetricWriteAck> createValueMetric(
            CreateValueMetricRequest request, RequestOptions requestOptions) {
        return this.rawClient.createValueMetric(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Returns one value metric with its formula, signal binding and monetary conversion joined together. Call getCurrentValueModel if you need the active version number to guard a follow-up write.
     */
    public CompletableFuture<ValueMetricDetail> getValueMetric(String slug) {
        return this.rawClient.getValueMetric(slug).thenApply(response -> response.body());
    }

    /**
     * Returns one value metric with its formula, signal binding and monetary conversion joined together. Call getCurrentValueModel if you need the active version number to guard a follow-up write.
     */
    public CompletableFuture<ValueMetricDetail> getValueMetric(String slug, GetValueMetricRequest request) {
        return this.rawClient.getValueMetric(slug, request).thenApply(response -> response.body());
    }

    /**
     * Returns one value metric with its formula, signal binding and monetary conversion joined together. Call getCurrentValueModel if you need the active version number to guard a follow-up write.
     */
    public CompletableFuture<ValueMetricDetail> getValueMetric(
            String slug, GetValueMetricRequest request, RequestOptions requestOptions) {
        return this.rawClient.getValueMetric(slug, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Marks a value metric archived so it stops appearing in listValueMetrics. Its formula and signal bindings are deliberately kept, so historical delivered value and sealed value receipts still resolve — which also means an archived metric's signals continue to be ingested and can still surface on value receipts. Removing it from receipts entirely requires deleting its signal bindings. Restore it with updateValueMetric and archivedAt null.
     */
    public CompletableFuture<ValueMetricWriteAck> archiveValueMetric(String slug) {
        return this.rawClient.archiveValueMetric(slug).thenApply(response -> response.body());
    }

    /**
     * Marks a value metric archived so it stops appearing in listValueMetrics. Its formula and signal bindings are deliberately kept, so historical delivered value and sealed value receipts still resolve — which also means an archived metric's signals continue to be ingested and can still surface on value receipts. Removing it from receipts entirely requires deleting its signal bindings. Restore it with updateValueMetric and archivedAt null.
     */
    public CompletableFuture<ValueMetricWriteAck> archiveValueMetric(String slug, ArchiveValueMetricRequest request) {
        return this.rawClient.archiveValueMetric(slug, request).thenApply(response -> response.body());
    }

    /**
     * Marks a value metric archived so it stops appearing in listValueMetrics. Its formula and signal bindings are deliberately kept, so historical delivered value and sealed value receipts still resolve — which also means an archived metric's signals continue to be ingested and can still surface on value receipts. Removing it from receipts entirely requires deleting its signal bindings. Restore it with updateValueMetric and archivedAt null.
     */
    public CompletableFuture<ValueMetricWriteAck> archiveValueMetric(
            String slug, ArchiveValueMetricRequest request, RequestOptions requestOptions) {
        return this.rawClient.archiveValueMetric(slug, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Changes one value metric. Omitted fields are left alone. You can change its name, category, unit, customer-facing copy, sources, monetary rate, archive state, and the value, label or display format of any variable its formula declares. The formula expression and the signal it is bound to cannot be changed — recreate the metric, or use the whole value-model upload. Publishes a new value model version.
     */
    public CompletableFuture<ValueMetricWriteAck> updateValueMetric(String slug) {
        return this.rawClient.updateValueMetric(slug).thenApply(response -> response.body());
    }

    /**
     * Changes one value metric. Omitted fields are left alone. You can change its name, category, unit, customer-facing copy, sources, monetary rate, archive state, and the value, label or display format of any variable its formula declares. The formula expression and the signal it is bound to cannot be changed — recreate the metric, or use the whole value-model upload. Publishes a new value model version.
     */
    public CompletableFuture<ValueMetricWriteAck> updateValueMetric(String slug, UpdateValueMetricRequest request) {
        return this.rawClient.updateValueMetric(slug, request).thenApply(response -> response.body());
    }

    /**
     * Changes one value metric. Omitted fields are left alone. You can change its name, category, unit, customer-facing copy, sources, monetary rate, archive state, and the value, label or display format of any variable its formula declares. The formula expression and the signal it is bound to cannot be changed — recreate the metric, or use the whole value-model upload. Publishes a new value model version.
     */
    public CompletableFuture<ValueMetricWriteAck> updateValueMetric(
            String slug, UpdateValueMetricRequest request, RequestOptions requestOptions) {
        return this.rawClient.updateValueMetric(slug, request, requestOptions).thenApply(response -> response.body());
    }
}
