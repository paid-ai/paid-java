/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.resources.deliveredvalue.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.ObjectMappers;
import com.paid.api.resources.deliveredvalue.types.GetDeliveredValueSummaryRequestGroupBy;
import com.paid.api.resources.deliveredvalue.types.GetDeliveredValueSummaryRequestInterval;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = GetDeliveredValueSummaryRequest.Builder.class)
public final class GetDeliveredValueSummaryRequest {
    private final Optional<String> customerId;

    private final Optional<String> externalCustomerId;

    private final String startDate;

    private final String endDate;

    private final GetDeliveredValueSummaryRequestGroupBy groupBy;

    private final Optional<String> dimensionKey;

    private final GetDeliveredValueSummaryRequestInterval interval;

    private final Map<String, Object> additionalProperties;

    private GetDeliveredValueSummaryRequest(
            Optional<String> customerId,
            Optional<String> externalCustomerId,
            String startDate,
            String endDate,
            GetDeliveredValueSummaryRequestGroupBy groupBy,
            Optional<String> dimensionKey,
            GetDeliveredValueSummaryRequestInterval interval,
            Map<String, Object> additionalProperties) {
        this.customerId = customerId;
        this.externalCustomerId = externalCustomerId;
        this.startDate = startDate;
        this.endDate = endDate;
        this.groupBy = groupBy;
        this.dimensionKey = dimensionKey;
        this.interval = interval;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The customer to summarize delivered value for. Accepts a Paid customer ID or, as a fallback, your external customer ID. Mutually exclusive with externalCustomerId; exactly one is required.
     */
    @JsonProperty("customerId")
    public Optional<String> getCustomerId() {
        return customerId;
    }

    /**
     * @return Your external customer ID. Mutually exclusive with customerId; exactly one is required.
     */
    @JsonProperty("externalCustomerId")
    public Optional<String> getExternalCustomerId() {
        return externalCustomerId;
    }

    /**
     * @return Window start, inclusive. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.
     */
    @JsonProperty("startDate")
    public String getStartDate() {
        return startDate;
    }

    /**
     * @return Window end, inclusive — a date-only value means through the end of that UTC day. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.
     */
    @JsonProperty("endDate")
    public String getEndDate() {
        return endDate;
    }

    /**
     * @return How to group the buckets. valueMetric groups by the value metric that delivered the money. dimension groups by a signal-payload key named in dimensionKey, summed across metrics — for example, value delivered per team when your signals carry a team_id field. formulaCondition cuts each metric by the condition band that priced it, for a value model that prices in graduated bands — for example, value delivered per workflow complexity when the metric bands runs by step count. A band the window never hit still reports a zero in every period, so the series keeps its shape.
     */
    @JsonProperty("groupBy")
    public GetDeliveredValueSummaryRequestGroupBy getGroupBy() {
        return groupBy;
    }

    /**
     * @return The signal-payload key to group by (up to 64 letters, digits, or underscores). Required when groupBy is dimension; rejected otherwise. A signal whose payload lacks the key, or holds it empty, groups under an empty groupKey.
     */
    @JsonProperty("dimensionKey")
    public Optional<String> getDimensionKey() {
        return dimensionKey;
    }

    /**
     * @return Period bucket size. Buckets align to UTC hours, calendar days, ISO weeks (Monday start), calendar months or calendar quarters; the first and last bucket's published bounds are clipped to the requested window. The window cannot hold more than 400 buckets.
     */
    @JsonProperty("interval")
    public GetDeliveredValueSummaryRequestInterval getInterval() {
        return interval;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof GetDeliveredValueSummaryRequest && equalTo((GetDeliveredValueSummaryRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(GetDeliveredValueSummaryRequest other) {
        return customerId.equals(other.customerId)
                && externalCustomerId.equals(other.externalCustomerId)
                && startDate.equals(other.startDate)
                && endDate.equals(other.endDate)
                && groupBy.equals(other.groupBy)
                && dimensionKey.equals(other.dimensionKey)
                && interval.equals(other.interval);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.customerId,
                this.externalCustomerId,
                this.startDate,
                this.endDate,
                this.groupBy,
                this.dimensionKey,
                this.interval);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static StartDateStage builder() {
        return new Builder();
    }

    public interface StartDateStage {
        /**
         * <p>Window start, inclusive. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.</p>
         */
        EndDateStage startDate(@NotNull String startDate);

        Builder from(GetDeliveredValueSummaryRequest other);
    }

    public interface EndDateStage {
        /**
         * <p>Window end, inclusive — a date-only value means through the end of that UTC day. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.</p>
         */
        GroupByStage endDate(@NotNull String endDate);
    }

    public interface GroupByStage {
        /**
         * <p>How to group the buckets. valueMetric groups by the value metric that delivered the money. dimension groups by a signal-payload key named in dimensionKey, summed across metrics — for example, value delivered per team when your signals carry a team_id field. formulaCondition cuts each metric by the condition band that priced it, for a value model that prices in graduated bands — for example, value delivered per workflow complexity when the metric bands runs by step count. A band the window never hit still reports a zero in every period, so the series keeps its shape.</p>
         */
        IntervalStage groupBy(@NotNull GetDeliveredValueSummaryRequestGroupBy groupBy);
    }

    public interface IntervalStage {
        /**
         * <p>Period bucket size. Buckets align to UTC hours, calendar days, ISO weeks (Monday start), calendar months or calendar quarters; the first and last bucket's published bounds are clipped to the requested window. The window cannot hold more than 400 buckets.</p>
         */
        _FinalStage interval(@NotNull GetDeliveredValueSummaryRequestInterval interval);
    }

    public interface _FinalStage {
        GetDeliveredValueSummaryRequest build();

        /**
         * <p>The customer to summarize delivered value for. Accepts a Paid customer ID or, as a fallback, your external customer ID. Mutually exclusive with externalCustomerId; exactly one is required.</p>
         */
        _FinalStage customerId(Optional<String> customerId);

        _FinalStage customerId(String customerId);

        /**
         * <p>Your external customer ID. Mutually exclusive with customerId; exactly one is required.</p>
         */
        _FinalStage externalCustomerId(Optional<String> externalCustomerId);

        _FinalStage externalCustomerId(String externalCustomerId);

        /**
         * <p>The signal-payload key to group by (up to 64 letters, digits, or underscores). Required when groupBy is dimension; rejected otherwise. A signal whose payload lacks the key, or holds it empty, groups under an empty groupKey.</p>
         */
        _FinalStage dimensionKey(Optional<String> dimensionKey);

        _FinalStage dimensionKey(String dimensionKey);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements StartDateStage, EndDateStage, GroupByStage, IntervalStage, _FinalStage {
        private String startDate;

        private String endDate;

        private GetDeliveredValueSummaryRequestGroupBy groupBy;

        private GetDeliveredValueSummaryRequestInterval interval;

        private Optional<String> dimensionKey = Optional.empty();

        private Optional<String> externalCustomerId = Optional.empty();

        private Optional<String> customerId = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(GetDeliveredValueSummaryRequest other) {
            customerId(other.getCustomerId());
            externalCustomerId(other.getExternalCustomerId());
            startDate(other.getStartDate());
            endDate(other.getEndDate());
            groupBy(other.getGroupBy());
            dimensionKey(other.getDimensionKey());
            interval(other.getInterval());
            return this;
        }

        /**
         * <p>Window start, inclusive. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.</p>
         * <p>Window start, inclusive. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("startDate")
        public EndDateStage startDate(@NotNull String startDate) {
            this.startDate = Objects.requireNonNull(startDate, "startDate must not be null");
            return this;
        }

        /**
         * <p>Window end, inclusive — a date-only value means through the end of that UTC day. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.</p>
         * <p>Window end, inclusive — a date-only value means through the end of that UTC day. Accepts an ISO 8601 date or date-time. Date-only values (e.g. 2026-06-30) are treated as UTC; date-times without an explicit timezone offset are ambiguous, so include one (e.g. 2026-06-30T00:00:00-05:00) when precision matters.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("endDate")
        public GroupByStage endDate(@NotNull String endDate) {
            this.endDate = Objects.requireNonNull(endDate, "endDate must not be null");
            return this;
        }

        /**
         * <p>How to group the buckets. valueMetric groups by the value metric that delivered the money. dimension groups by a signal-payload key named in dimensionKey, summed across metrics — for example, value delivered per team when your signals carry a team_id field. formulaCondition cuts each metric by the condition band that priced it, for a value model that prices in graduated bands — for example, value delivered per workflow complexity when the metric bands runs by step count. A band the window never hit still reports a zero in every period, so the series keeps its shape.</p>
         * <p>How to group the buckets. valueMetric groups by the value metric that delivered the money. dimension groups by a signal-payload key named in dimensionKey, summed across metrics — for example, value delivered per team when your signals carry a team_id field. formulaCondition cuts each metric by the condition band that priced it, for a value model that prices in graduated bands — for example, value delivered per workflow complexity when the metric bands runs by step count. A band the window never hit still reports a zero in every period, so the series keeps its shape.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("groupBy")
        public IntervalStage groupBy(@NotNull GetDeliveredValueSummaryRequestGroupBy groupBy) {
            this.groupBy = Objects.requireNonNull(groupBy, "groupBy must not be null");
            return this;
        }

        /**
         * <p>Period bucket size. Buckets align to UTC hours, calendar days, ISO weeks (Monday start), calendar months or calendar quarters; the first and last bucket's published bounds are clipped to the requested window. The window cannot hold more than 400 buckets.</p>
         * <p>Period bucket size. Buckets align to UTC hours, calendar days, ISO weeks (Monday start), calendar months or calendar quarters; the first and last bucket's published bounds are clipped to the requested window. The window cannot hold more than 400 buckets.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("interval")
        public _FinalStage interval(@NotNull GetDeliveredValueSummaryRequestInterval interval) {
            this.interval = Objects.requireNonNull(interval, "interval must not be null");
            return this;
        }

        /**
         * <p>The signal-payload key to group by (up to 64 letters, digits, or underscores). Required when groupBy is dimension; rejected otherwise. A signal whose payload lacks the key, or holds it empty, groups under an empty groupKey.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage dimensionKey(String dimensionKey) {
            this.dimensionKey = Optional.ofNullable(dimensionKey);
            return this;
        }

        /**
         * <p>The signal-payload key to group by (up to 64 letters, digits, or underscores). Required when groupBy is dimension; rejected otherwise. A signal whose payload lacks the key, or holds it empty, groups under an empty groupKey.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "dimensionKey", nulls = Nulls.SKIP)
        public _FinalStage dimensionKey(Optional<String> dimensionKey) {
            this.dimensionKey = dimensionKey;
            return this;
        }

        /**
         * <p>Your external customer ID. Mutually exclusive with customerId; exactly one is required.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage externalCustomerId(String externalCustomerId) {
            this.externalCustomerId = Optional.ofNullable(externalCustomerId);
            return this;
        }

        /**
         * <p>Your external customer ID. Mutually exclusive with customerId; exactly one is required.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "externalCustomerId", nulls = Nulls.SKIP)
        public _FinalStage externalCustomerId(Optional<String> externalCustomerId) {
            this.externalCustomerId = externalCustomerId;
            return this;
        }

        /**
         * <p>The customer to summarize delivered value for. Accepts a Paid customer ID or, as a fallback, your external customer ID. Mutually exclusive with externalCustomerId; exactly one is required.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage customerId(String customerId) {
            this.customerId = Optional.ofNullable(customerId);
            return this;
        }

        /**
         * <p>The customer to summarize delivered value for. Accepts a Paid customer ID or, as a fallback, your external customer ID. Mutually exclusive with externalCustomerId; exactly one is required.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "customerId", nulls = Nulls.SKIP)
        public _FinalStage customerId(Optional<String> customerId) {
            this.customerId = customerId;
            return this;
        }

        @java.lang.Override
        public GetDeliveredValueSummaryRequest build() {
            return new GetDeliveredValueSummaryRequest(
                    customerId,
                    externalCustomerId,
                    startDate,
                    endDate,
                    groupBy,
                    dimensionKey,
                    interval,
                    additionalProperties);
        }
    }
}
