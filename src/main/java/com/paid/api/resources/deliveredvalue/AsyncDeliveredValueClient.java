/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.resources.deliveredvalue;

import com.paid.api.core.ClientOptions;
import com.paid.api.core.RequestOptions;
import com.paid.api.resources.deliveredvalue.requests.GetDeliveredValueRequest;
import com.paid.api.resources.deliveredvalue.requests.GetDeliveredValueSummaryRequest;
import com.paid.api.types.DeliveredValueResponse;
import com.paid.api.types.DeliveredValueSummaryResponse;
import java.util.concurrent.CompletableFuture;

public class AsyncDeliveredValueClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawDeliveredValueClient rawClient;

    public AsyncDeliveredValueClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawDeliveredValueClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawDeliveredValueClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Returns the value delivered to a customer over a period, computed live from your active value model on every call — per value metric: its definition, a display-ready calculation, the measured unit total, monetary total, and signal counts, plus a per-event signal breakdown. The same data a value receipt presents, without creating one — built for value dashboards and reporting. Figures always reflect the currently active value model. Shortly after you publish a model change, totals can still be priced under the previous model's rates.
     */
    public CompletableFuture<DeliveredValueResponse> getDeliveredValue(GetDeliveredValueRequest request) {
        return this.rawClient.getDeliveredValue(request).thenApply(response -> response.body());
    }

    /**
     * Returns the value delivered to a customer over a period, computed live from your active value model on every call — per value metric: its definition, a display-ready calculation, the measured unit total, monetary total, and signal counts, plus a per-event signal breakdown. The same data a value receipt presents, without creating one — built for value dashboards and reporting. Figures always reflect the currently active value model. Shortly after you publish a model change, totals can still be priced under the previous model's rates.
     */
    public CompletableFuture<DeliveredValueResponse> getDeliveredValue(
            GetDeliveredValueRequest request, RequestOptions requestOptions) {
        return this.rawClient.getDeliveredValue(request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Aggregates delivered value into period buckets for charting value over time, grouped by value metric, by a signal-payload key (for example, value delivered per team_id), or by the formula condition band that priced each row (for example, value delivered per workflow complexity). Reads the same rows as GET /delivered-value, so summing every bucket's monetaryTotal matches that endpoint's total for the same customer and window. Figures always reflect the currently active value model. Shortly after you publish a model change, totals can still be priced under the previous model's rates. Grouping by band reads a figure recorded when the row was priced, so value delivered before this grouping existed reports under the &quot;none&quot; band until a value-model backfill re-derives it.
     */
    public CompletableFuture<DeliveredValueSummaryResponse> getDeliveredValueSummary(
            GetDeliveredValueSummaryRequest request) {
        return this.rawClient.getDeliveredValueSummary(request).thenApply(response -> response.body());
    }

    /**
     * Aggregates delivered value into period buckets for charting value over time, grouped by value metric, by a signal-payload key (for example, value delivered per team_id), or by the formula condition band that priced each row (for example, value delivered per workflow complexity). Reads the same rows as GET /delivered-value, so summing every bucket's monetaryTotal matches that endpoint's total for the same customer and window. Figures always reflect the currently active value model. Shortly after you publish a model change, totals can still be priced under the previous model's rates. Grouping by band reads a figure recorded when the row was priced, so value delivered before this grouping existed reports under the &quot;none&quot; band until a value-model backfill re-derives it.
     */
    public CompletableFuture<DeliveredValueSummaryResponse> getDeliveredValueSummary(
            GetDeliveredValueSummaryRequest request, RequestOptions requestOptions) {
        return this.rawClient.getDeliveredValueSummary(request, requestOptions).thenApply(response -> response.body());
    }
}
