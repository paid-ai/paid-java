/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.resources.deliveredvalue;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.paid.api.core.ClientOptions;
import com.paid.api.core.ObjectMappers;
import com.paid.api.core.PaidApiApiException;
import com.paid.api.core.PaidApiException;
import com.paid.api.core.PaidApiHttpResponse;
import com.paid.api.core.QueryStringMapper;
import com.paid.api.core.RequestOptions;
import com.paid.api.errors.BadRequestError;
import com.paid.api.errors.ForbiddenError;
import com.paid.api.errors.InternalServerError;
import com.paid.api.errors.NotFoundError;
import com.paid.api.errors.RequestTimeoutError;
import com.paid.api.errors.TooManyRequestsError;
import com.paid.api.resources.deliveredvalue.requests.GetDeliveredValueRequest;
import com.paid.api.resources.deliveredvalue.requests.GetDeliveredValueSummaryRequest;
import com.paid.api.types.DeliveredValueResponse;
import com.paid.api.types.DeliveredValueSummaryResponse;
import com.paid.api.types.ErrorResponse;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawDeliveredValueClient {
    protected final ClientOptions clientOptions;

    public RawDeliveredValueClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns the value delivered to a customer over a period, computed live from your active value model on every call — per value metric: its definition, a display-ready calculation, the measured unit total, monetary total, and signal counts, plus a per-event signal breakdown. The same data a value receipt presents, without creating one — built for value dashboards and reporting. Figures always reflect the currently active value model. Shortly after you publish a model change, totals can still be priced under the previous model's rates.
     */
    public PaidApiHttpResponse<DeliveredValueResponse> getDeliveredValue(GetDeliveredValueRequest request) {
        return getDeliveredValue(request, null);
    }

    /**
     * Returns the value delivered to a customer over a period, computed live from your active value model on every call — per value metric: its definition, a display-ready calculation, the measured unit total, monetary total, and signal counts, plus a per-event signal breakdown. The same data a value receipt presents, without creating one — built for value dashboards and reporting. Figures always reflect the currently active value model. Shortly after you publish a model change, totals can still be priced under the previous model's rates.
     */
    public PaidApiHttpResponse<DeliveredValueResponse> getDeliveredValue(
            GetDeliveredValueRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("delivered-value");
        if (request.getCustomerId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "customerId", request.getCustomerId().get(), false);
        }
        if (request.getExternalCustomerId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "externalCustomerId",
                    request.getExternalCustomerId().get(),
                    false);
        }
        QueryStringMapper.addQueryParameter(httpUrl, "startDate", request.getStartDate(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "endDate", request.getEndDate(), false);
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new PaidApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), DeliveredValueResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 408:
                        throw new RequestTimeoutError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            throw new PaidApiApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new PaidApiException("Network error executing HTTP request", e);
        }
    }

    /**
     * Aggregates delivered value into period buckets for charting value over time, grouped by value metric, by a signal-payload key (for example, value delivered per team_id), or by the formula condition band that priced each row (for example, value delivered per workflow complexity). Reads the same rows as GET /delivered-value, so summing every bucket's monetaryTotal matches that endpoint's total for the same customer and window. Figures always reflect the currently active value model. Shortly after you publish a model change, totals can still be priced under the previous model's rates. Grouping by band reads a figure recorded when the row was priced, so value delivered before this grouping existed reports under the &quot;none&quot; band until a value-model backfill re-derives it.
     */
    public PaidApiHttpResponse<DeliveredValueSummaryResponse> getDeliveredValueSummary(
            GetDeliveredValueSummaryRequest request) {
        return getDeliveredValueSummary(request, null);
    }

    /**
     * Aggregates delivered value into period buckets for charting value over time, grouped by value metric, by a signal-payload key (for example, value delivered per team_id), or by the formula condition band that priced each row (for example, value delivered per workflow complexity). Reads the same rows as GET /delivered-value, so summing every bucket's monetaryTotal matches that endpoint's total for the same customer and window. Figures always reflect the currently active value model. Shortly after you publish a model change, totals can still be priced under the previous model's rates. Grouping by band reads a figure recorded when the row was priced, so value delivered before this grouping existed reports under the &quot;none&quot; band until a value-model backfill re-derives it.
     */
    public PaidApiHttpResponse<DeliveredValueSummaryResponse> getDeliveredValueSummary(
            GetDeliveredValueSummaryRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("delivered-value/summary");
        if (request.getCustomerId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "customerId", request.getCustomerId().get(), false);
        }
        if (request.getExternalCustomerId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "externalCustomerId",
                    request.getExternalCustomerId().get(),
                    false);
        }
        QueryStringMapper.addQueryParameter(httpUrl, "startDate", request.getStartDate(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "endDate", request.getEndDate(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "groupBy", request.getGroupBy(), false);
        if (request.getDimensionKey().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "dimensionKey", request.getDimensionKey().get(), false);
        }
        QueryStringMapper.addQueryParameter(httpUrl, "interval", request.getInterval(), false);
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new PaidApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), DeliveredValueSummaryResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 408:
                        throw new RequestTimeoutError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            throw new PaidApiApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new PaidApiException("Network error executing HTTP request", e);
        }
    }
}
