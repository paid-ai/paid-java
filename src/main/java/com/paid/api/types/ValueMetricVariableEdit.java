/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.Nullable;
import com.paid.api.core.NullableNonemptyFilter;
import com.paid.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = ValueMetricVariableEdit.Builder.class)
public final class ValueMetricVariableEdit {
    private final Optional<Double> value;

    private final Optional<String> label;

    private final Optional<ValueMetricVariableEditFormat> format;

    private final Optional<ValueMetricVariableEditFormatUnit> formatUnit;

    private final Map<String, Object> additionalProperties;

    private ValueMetricVariableEdit(
            Optional<Double> value,
            Optional<String> label,
            Optional<ValueMetricVariableEditFormat> format,
            Optional<ValueMetricVariableEditFormatUnit> formatUnit,
            Map<String, Object> additionalProperties) {
        this.value = value;
        this.label = label;
        this.format = format;
        this.formatUnit = formatUnit;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return What this variable is worth for one occurrence of the signal. Applied to every binding of the metric whose formula declares it, matching Value Studio's updateConstantDefault.
     */
    @JsonProperty("value")
    public Optional<Double> getValue() {
        return value;
    }

    /**
     * @return Customer-facing name for this variable on a receipt's calculation strip.
     */
    @JsonProperty("label")
    public Optional<String> getLabel() {
        return label;
    }

    /**
     * @return Display-only. 'percent' renders a stored 0.02 as 2%. 'duration' requires formatUnit. Null clears the format. Calculations always use the stored raw value.
     */
    @JsonIgnore
    public Optional<ValueMetricVariableEditFormat> getFormat() {
        if (format == null) {
            return Optional.empty();
        }
        return format;
    }

    /**
     * @return The unit a 'duration' format is expressed in. Required when format is 'duration'.
     */
    @JsonIgnore
    public Optional<ValueMetricVariableEditFormatUnit> getFormatUnit() {
        if (formatUnit == null) {
            return Optional.empty();
        }
        return formatUnit;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("format")
    private Optional<ValueMetricVariableEditFormat> _getFormat() {
        return format;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("formatUnit")
    private Optional<ValueMetricVariableEditFormatUnit> _getFormatUnit() {
        return formatUnit;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ValueMetricVariableEdit && equalTo((ValueMetricVariableEdit) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(ValueMetricVariableEdit other) {
        return value.equals(other.value)
                && label.equals(other.label)
                && format.equals(other.format)
                && formatUnit.equals(other.formatUnit);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.value, this.label, this.format, this.formatUnit);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static Builder builder() {
        return new Builder();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder {
        private Optional<Double> value = Optional.empty();

        private Optional<String> label = Optional.empty();

        private Optional<ValueMetricVariableEditFormat> format = Optional.empty();

        private Optional<ValueMetricVariableEditFormatUnit> formatUnit = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        public Builder from(ValueMetricVariableEdit other) {
            value(other.getValue());
            label(other.getLabel());
            format(other.getFormat());
            formatUnit(other.getFormatUnit());
            return this;
        }

        /**
         * <p>What this variable is worth for one occurrence of the signal. Applied to every binding of the metric whose formula declares it, matching Value Studio's updateConstantDefault.</p>
         */
        @JsonSetter(value = "value", nulls = Nulls.SKIP)
        public Builder value(Optional<Double> value) {
            this.value = value;
            return this;
        }

        public Builder value(Double value) {
            this.value = Optional.ofNullable(value);
            return this;
        }

        /**
         * <p>Customer-facing name for this variable on a receipt's calculation strip.</p>
         */
        @JsonSetter(value = "label", nulls = Nulls.SKIP)
        public Builder label(Optional<String> label) {
            this.label = label;
            return this;
        }

        public Builder label(String label) {
            this.label = Optional.ofNullable(label);
            return this;
        }

        /**
         * <p>Display-only. 'percent' renders a stored 0.02 as 2%. 'duration' requires formatUnit. Null clears the format. Calculations always use the stored raw value.</p>
         */
        @JsonSetter(value = "format", nulls = Nulls.SKIP)
        public Builder format(Optional<ValueMetricVariableEditFormat> format) {
            this.format = format;
            return this;
        }

        public Builder format(ValueMetricVariableEditFormat format) {
            this.format = Optional.ofNullable(format);
            return this;
        }

        public Builder format(Nullable<ValueMetricVariableEditFormat> format) {
            if (format.isNull()) {
                this.format = null;
            } else if (format.isEmpty()) {
                this.format = Optional.empty();
            } else {
                this.format = Optional.of(format.get());
            }
            return this;
        }

        /**
         * <p>The unit a 'duration' format is expressed in. Required when format is 'duration'.</p>
         */
        @JsonSetter(value = "formatUnit", nulls = Nulls.SKIP)
        public Builder formatUnit(Optional<ValueMetricVariableEditFormatUnit> formatUnit) {
            this.formatUnit = formatUnit;
            return this;
        }

        public Builder formatUnit(ValueMetricVariableEditFormatUnit formatUnit) {
            this.formatUnit = Optional.ofNullable(formatUnit);
            return this;
        }

        public Builder formatUnit(Nullable<ValueMetricVariableEditFormatUnit> formatUnit) {
            if (formatUnit.isNull()) {
                this.formatUnit = null;
            } else if (formatUnit.isEmpty()) {
                this.formatUnit = Optional.empty();
            } else {
                this.formatUnit = Optional.of(formatUnit.get());
            }
            return this;
        }

        public ValueMetricVariableEdit build() {
            return new ValueMetricVariableEdit(value, label, format, formatUnit, additionalProperties);
        }
    }
}
