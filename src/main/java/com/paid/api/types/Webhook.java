/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.Nullable;
import com.paid.api.core.NullableNonemptyFilter;
import com.paid.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = Webhook.Builder.class)
public final class Webhook {
    private final String id;

    private final WebhookName name;

    private final String description;

    private final boolean enabled;

    private final Optional<String> url;

    private final Optional<OffsetDateTime> lastSentAt;

    private final Optional<WebhookDeliveryStatus> lastStatus;

    private final Map<String, Object> additionalProperties;

    private Webhook(
            String id,
            WebhookName name,
            String description,
            boolean enabled,
            Optional<String> url,
            Optional<OffsetDateTime> lastSentAt,
            Optional<WebhookDeliveryStatus> lastStatus,
            Map<String, Object> additionalProperties) {
        this.id = id;
        this.name = name;
        this.description = description;
        this.enabled = enabled;
        this.url = url;
        this.lastSentAt = lastSentAt;
        this.lastStatus = lastStatus;
        this.additionalProperties = additionalProperties;
    }

    @JsonProperty("id")
    public String getId() {
        return id;
    }

    @JsonProperty("name")
    public WebhookName getName() {
        return name;
    }

    @JsonProperty("description")
    public String getDescription() {
        return description;
    }

    @JsonProperty("enabled")
    public boolean getEnabled() {
        return enabled;
    }

    @JsonIgnore
    public Optional<String> getUrl() {
        if (url == null) {
            return Optional.empty();
        }
        return url;
    }

    @JsonIgnore
    public Optional<OffsetDateTime> getLastSentAt() {
        if (lastSentAt == null) {
            return Optional.empty();
        }
        return lastSentAt;
    }

    @JsonIgnore
    public Optional<WebhookDeliveryStatus> getLastStatus() {
        if (lastStatus == null) {
            return Optional.empty();
        }
        return lastStatus;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("url")
    private Optional<String> _getUrl() {
        return url;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("lastSentAt")
    private Optional<OffsetDateTime> _getLastSentAt() {
        return lastSentAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("lastStatus")
    private Optional<WebhookDeliveryStatus> _getLastStatus() {
        return lastStatus;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof Webhook && equalTo((Webhook) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(Webhook other) {
        return id.equals(other.id)
                && name.equals(other.name)
                && description.equals(other.description)
                && enabled == other.enabled
                && url.equals(other.url)
                && lastSentAt.equals(other.lastSentAt)
                && lastStatus.equals(other.lastStatus);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.id, this.name, this.description, this.enabled, this.url, this.lastSentAt, this.lastStatus);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        NameStage id(@NotNull String id);

        Builder from(Webhook other);
    }

    public interface NameStage {
        DescriptionStage name(@NotNull WebhookName name);
    }

    public interface DescriptionStage {
        EnabledStage description(@NotNull String description);
    }

    public interface EnabledStage {
        _FinalStage enabled(boolean enabled);
    }

    public interface _FinalStage {
        Webhook build();

        _FinalStage url(Optional<String> url);

        _FinalStage url(String url);

        _FinalStage url(Nullable<String> url);

        _FinalStage lastSentAt(Optional<OffsetDateTime> lastSentAt);

        _FinalStage lastSentAt(OffsetDateTime lastSentAt);

        _FinalStage lastSentAt(Nullable<OffsetDateTime> lastSentAt);

        _FinalStage lastStatus(Optional<WebhookDeliveryStatus> lastStatus);

        _FinalStage lastStatus(WebhookDeliveryStatus lastStatus);

        _FinalStage lastStatus(Nullable<WebhookDeliveryStatus> lastStatus);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, NameStage, DescriptionStage, EnabledStage, _FinalStage {
        private String id;

        private WebhookName name;

        private String description;

        private boolean enabled;

        private Optional<WebhookDeliveryStatus> lastStatus = Optional.empty();

        private Optional<OffsetDateTime> lastSentAt = Optional.empty();

        private Optional<String> url = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(Webhook other) {
            id(other.getId());
            name(other.getName());
            description(other.getDescription());
            enabled(other.getEnabled());
            url(other.getUrl());
            lastSentAt(other.getLastSentAt());
            lastStatus(other.getLastStatus());
            return this;
        }

        @java.lang.Override
        @JsonSetter("id")
        public NameStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("name")
        public DescriptionStage name(@NotNull WebhookName name) {
            this.name = Objects.requireNonNull(name, "name must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("description")
        public EnabledStage description(@NotNull String description) {
            this.description = Objects.requireNonNull(description, "description must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("enabled")
        public _FinalStage enabled(boolean enabled) {
            this.enabled = enabled;
            return this;
        }

        @java.lang.Override
        public _FinalStage lastStatus(Nullable<WebhookDeliveryStatus> lastStatus) {
            if (lastStatus.isNull()) {
                this.lastStatus = null;
            } else if (lastStatus.isEmpty()) {
                this.lastStatus = Optional.empty();
            } else {
                this.lastStatus = Optional.of(lastStatus.get());
            }
            return this;
        }

        @java.lang.Override
        public _FinalStage lastStatus(WebhookDeliveryStatus lastStatus) {
            this.lastStatus = Optional.ofNullable(lastStatus);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "lastStatus", nulls = Nulls.SKIP)
        public _FinalStage lastStatus(Optional<WebhookDeliveryStatus> lastStatus) {
            this.lastStatus = lastStatus;
            return this;
        }

        @java.lang.Override
        public _FinalStage lastSentAt(Nullable<OffsetDateTime> lastSentAt) {
            if (lastSentAt.isNull()) {
                this.lastSentAt = null;
            } else if (lastSentAt.isEmpty()) {
                this.lastSentAt = Optional.empty();
            } else {
                this.lastSentAt = Optional.of(lastSentAt.get());
            }
            return this;
        }

        @java.lang.Override
        public _FinalStage lastSentAt(OffsetDateTime lastSentAt) {
            this.lastSentAt = Optional.ofNullable(lastSentAt);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "lastSentAt", nulls = Nulls.SKIP)
        public _FinalStage lastSentAt(Optional<OffsetDateTime> lastSentAt) {
            this.lastSentAt = lastSentAt;
            return this;
        }

        @java.lang.Override
        public _FinalStage url(Nullable<String> url) {
            if (url.isNull()) {
                this.url = null;
            } else if (url.isEmpty()) {
                this.url = Optional.empty();
            } else {
                this.url = Optional.of(url.get());
            }
            return this;
        }

        @java.lang.Override
        public _FinalStage url(String url) {
            this.url = Optional.ofNullable(url);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "url", nulls = Nulls.SKIP)
        public _FinalStage url(Optional<String> url) {
            this.url = url;
            return this;
        }

        @java.lang.Override
        public Webhook build() {
            return new Webhook(id, name, description, enabled, url, lastSentAt, lastStatus, additionalProperties);
        }
    }
}
