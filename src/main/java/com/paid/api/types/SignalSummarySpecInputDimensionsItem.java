/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class SignalSummarySpecInputDimensionsItem {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private SignalSummarySpecInputDimensionsItem(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static SignalSummarySpecInputDimensionsItem presence(SignalSummaryPresenceDimension value) {
        return new SignalSummarySpecInputDimensionsItem(new PresenceValue(value));
    }

    public static SignalSummarySpecInputDimensionsItem bands(SignalSummaryBandsDimension value) {
        return new SignalSummarySpecInputDimensionsItem(new BandsValue(value));
    }

    public boolean isPresence() {
        return value instanceof PresenceValue;
    }

    public boolean isBands() {
        return value instanceof BandsValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<SignalSummaryPresenceDimension> getPresence() {
        if (isPresence()) {
            return Optional.of(((PresenceValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<SignalSummaryBandsDimension> getBands() {
        if (isBands()) {
            return Optional.of(((BandsValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitPresence(SignalSummaryPresenceDimension presence);

        T visitBands(SignalSummaryBandsDimension bands);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "kind", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(PresenceValue.class), @JsonSubTypes.Type(BandsValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("presence")
    @JsonIgnoreProperties("kind")
    private static final class PresenceValue implements Value {
        @JsonUnwrapped
        private SignalSummaryPresenceDimension value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PresenceValue() {}

        private PresenceValue(SignalSummaryPresenceDimension value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPresence(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PresenceValue && equalTo((PresenceValue) other);
        }

        private boolean equalTo(PresenceValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SignalSummarySpecInputDimensionsItem{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("bands")
    @JsonIgnoreProperties("kind")
    private static final class BandsValue implements Value {
        @JsonUnwrapped
        private SignalSummaryBandsDimension value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private BandsValue() {}

        private BandsValue(SignalSummaryBandsDimension value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitBands(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof BandsValue && equalTo((BandsValue) other);
        }

        private boolean equalTo(BandsValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SignalSummarySpecInputDimensionsItem{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("kind")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SignalSummarySpecInputDimensionsItem{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
