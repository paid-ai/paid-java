/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class Cost {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private Cost(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static Cost cost(PrecomputedCost value) {
        return new Cost(new CostValue(value));
    }

    public static Cost usage(UsageCost value) {
        return new Cost(new UsageValue(value));
    }

    public boolean isCost() {
        return value instanceof CostValue;
    }

    public boolean isUsage() {
        return value instanceof UsageValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<PrecomputedCost> getCost() {
        if (isCost()) {
            return Optional.of(((CostValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<UsageCost> getUsage() {
        if (isUsage()) {
            return Optional.of(((UsageValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitCost(PrecomputedCost cost);

        T visitUsage(UsageCost usage);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(CostValue.class), @JsonSubTypes.Type(UsageValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("cost")
    @JsonIgnoreProperties("type")
    private static final class CostValue implements Value {
        @JsonUnwrapped
        private PrecomputedCost value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CostValue() {}

        private CostValue(PrecomputedCost value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCost(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CostValue && equalTo((CostValue) other);
        }

        private boolean equalTo(CostValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "Cost{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("usage")
    @JsonIgnoreProperties("type")
    private static final class UsageValue implements Value {
        @JsonUnwrapped
        private UsageCost value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private UsageValue() {}

        private UsageValue(UsageCost value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitUsage(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof UsageValue && equalTo((UsageValue) other);
        }

        private boolean equalTo(UsageValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "Cost{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "Cost{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
