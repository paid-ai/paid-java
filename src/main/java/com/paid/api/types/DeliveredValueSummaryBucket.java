/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = DeliveredValueSummaryBucket.Builder.class)
public final class DeliveredValueSummaryBucket {
    private final OffsetDateTime periodStart;

    private final OffsetDateTime periodEnd;

    private final String groupKey;

    private final Optional<String> valueMetric;

    private final Optional<Double> conditionIndex;

    private final Optional<String> conditionLabel;

    private final Optional<Double> unitTotal;

    private final double monetaryTotal;

    private final double signalCount;

    private final Map<String, Object> additionalProperties;

    private DeliveredValueSummaryBucket(
            OffsetDateTime periodStart,
            OffsetDateTime periodEnd,
            String groupKey,
            Optional<String> valueMetric,
            Optional<Double> conditionIndex,
            Optional<String> conditionLabel,
            Optional<Double> unitTotal,
            double monetaryTotal,
            double signalCount,
            Map<String, Object> additionalProperties) {
        this.periodStart = periodStart;
        this.periodEnd = periodEnd;
        this.groupKey = groupKey;
        this.valueMetric = valueMetric;
        this.conditionIndex = conditionIndex;
        this.conditionLabel = conditionLabel;
        this.unitTotal = unitTotal;
        this.monetaryTotal = monetaryTotal;
        this.signalCount = signalCount;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Start of the bucket (inclusive), UTC. Clipped to the requested startDate when the window starts mid-period.
     */
    @JsonProperty("periodStart")
    public OffsetDateTime getPeriodStart() {
        return periodStart;
    }

    /**
     * @return Last instant of the bucket (inclusive), UTC. Clipped to the requested endDate when the window ends mid-period.
     */
    @JsonProperty("periodEnd")
    public OffsetDateTime getPeriodEnd() {
        return periodEnd;
    }

    /**
     * @return The group this bucket belongs to. For valueMetric grouping, the metric's slug; an archived metric still appears when it delivered value in the window. For dimension grouping, the payload value of dimensionKey; a signal whose payload lacks the key, or holds it empty, groups under the empty string. For formulaCondition grouping, the metric's slug, a colon, and the band: the zero-based index of the condition that priced the rows, &quot;default&quot; where every condition missed, or &quot;none&quot; where the metric priced them through a formula that has no conditions. Read valueMetric and conditionIndex rather than parsing this.
     */
    @JsonProperty("groupKey")
    public String getGroupKey() {
        return groupKey;
    }

    /**
     * @return Slug of the metric this band belongs to. Present only under formulaCondition grouping.
     */
    @JsonProperty("valueMetric")
    public Optional<String> getValueMetric() {
        return valueMetric;
    }

    /**
     * @return Zero-based index of the formula condition that priced this band's rows, in the order the value model authors them. Present only under formulaCondition grouping, and only for a band that is a condition — absent for the &quot;default&quot; and &quot;none&quot; groups.
     */
    @JsonProperty("conditionIndex")
    public Optional<Double> getConditionIndex() {
        return conditionIndex;
    }

    /**
     * @return The condition that priced this band, as field, operator and value — a stable descriptor, not display copy. A metric priced through several formulas states the label they agree on; where they band differently at this index, no one condition describes the group and the label is absent.
     */
    @JsonProperty("conditionLabel")
    public Optional<String> getConditionLabel() {
        return conditionLabel;
    }

    /**
     * @return Value delivered in this bucket in the metric's own unit — minutes saved, tickets resolved — as GET /delivered-value states it. Present under valueMetric and formulaCondition grouping, where every row in a group belongs to one metric and so shares its unit. Absent under dimension grouping, which sums across metrics that need not measure the same thing.
     */
    @JsonProperty("unitTotal")
    public Optional<Double> getUnitTotal() {
        return unitTotal;
    }

    /**
     * @return Money delivered in this bucket, in decimal units of the response currency (12.5 = $12.50), not cents.
     */
    @JsonProperty("monetaryTotal")
    public double getMonetaryTotal() {
        return monetaryTotal;
    }

    /**
     * @return Signals that contributed to this group in this bucket, counted by quantity — one signal reporting a batch of 100 counts as 100. For valueMetric grouping, a signal feeding two metrics counts under both groups. For dimension grouping, it counts once in its group.
     */
    @JsonProperty("signalCount")
    public double getSignalCount() {
        return signalCount;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof DeliveredValueSummaryBucket && equalTo((DeliveredValueSummaryBucket) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(DeliveredValueSummaryBucket other) {
        return periodStart.equals(other.periodStart)
                && periodEnd.equals(other.periodEnd)
                && groupKey.equals(other.groupKey)
                && valueMetric.equals(other.valueMetric)
                && conditionIndex.equals(other.conditionIndex)
                && conditionLabel.equals(other.conditionLabel)
                && unitTotal.equals(other.unitTotal)
                && monetaryTotal == other.monetaryTotal
                && signalCount == other.signalCount;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.periodStart,
                this.periodEnd,
                this.groupKey,
                this.valueMetric,
                this.conditionIndex,
                this.conditionLabel,
                this.unitTotal,
                this.monetaryTotal,
                this.signalCount);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static PeriodStartStage builder() {
        return new Builder();
    }

    public interface PeriodStartStage {
        /**
         * <p>Start of the bucket (inclusive), UTC. Clipped to the requested startDate when the window starts mid-period.</p>
         */
        PeriodEndStage periodStart(@NotNull OffsetDateTime periodStart);

        Builder from(DeliveredValueSummaryBucket other);
    }

    public interface PeriodEndStage {
        /**
         * <p>Last instant of the bucket (inclusive), UTC. Clipped to the requested endDate when the window ends mid-period.</p>
         */
        GroupKeyStage periodEnd(@NotNull OffsetDateTime periodEnd);
    }

    public interface GroupKeyStage {
        /**
         * <p>The group this bucket belongs to. For valueMetric grouping, the metric's slug; an archived metric still appears when it delivered value in the window. For dimension grouping, the payload value of dimensionKey; a signal whose payload lacks the key, or holds it empty, groups under the empty string. For formulaCondition grouping, the metric's slug, a colon, and the band: the zero-based index of the condition that priced the rows, &quot;default&quot; where every condition missed, or &quot;none&quot; where the metric priced them through a formula that has no conditions. Read valueMetric and conditionIndex rather than parsing this.</p>
         */
        MonetaryTotalStage groupKey(@NotNull String groupKey);
    }

    public interface MonetaryTotalStage {
        /**
         * <p>Money delivered in this bucket, in decimal units of the response currency (12.5 = $12.50), not cents.</p>
         */
        SignalCountStage monetaryTotal(double monetaryTotal);
    }

    public interface SignalCountStage {
        /**
         * <p>Signals that contributed to this group in this bucket, counted by quantity — one signal reporting a batch of 100 counts as 100. For valueMetric grouping, a signal feeding two metrics counts under both groups. For dimension grouping, it counts once in its group.</p>
         */
        _FinalStage signalCount(double signalCount);
    }

    public interface _FinalStage {
        DeliveredValueSummaryBucket build();

        /**
         * <p>Slug of the metric this band belongs to. Present only under formulaCondition grouping.</p>
         */
        _FinalStage valueMetric(Optional<String> valueMetric);

        _FinalStage valueMetric(String valueMetric);

        /**
         * <p>Zero-based index of the formula condition that priced this band's rows, in the order the value model authors them. Present only under formulaCondition grouping, and only for a band that is a condition — absent for the &quot;default&quot; and &quot;none&quot; groups.</p>
         */
        _FinalStage conditionIndex(Optional<Double> conditionIndex);

        _FinalStage conditionIndex(Double conditionIndex);

        /**
         * <p>The condition that priced this band, as field, operator and value — a stable descriptor, not display copy. A metric priced through several formulas states the label they agree on; where they band differently at this index, no one condition describes the group and the label is absent.</p>
         */
        _FinalStage conditionLabel(Optional<String> conditionLabel);

        _FinalStage conditionLabel(String conditionLabel);

        /**
         * <p>Value delivered in this bucket in the metric's own unit — minutes saved, tickets resolved — as GET /delivered-value states it. Present under valueMetric and formulaCondition grouping, where every row in a group belongs to one metric and so shares its unit. Absent under dimension grouping, which sums across metrics that need not measure the same thing.</p>
         */
        _FinalStage unitTotal(Optional<Double> unitTotal);

        _FinalStage unitTotal(Double unitTotal);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements PeriodStartStage,
                    PeriodEndStage,
                    GroupKeyStage,
                    MonetaryTotalStage,
                    SignalCountStage,
                    _FinalStage {
        private OffsetDateTime periodStart;

        private OffsetDateTime periodEnd;

        private String groupKey;

        private double monetaryTotal;

        private double signalCount;

        private Optional<Double> unitTotal = Optional.empty();

        private Optional<String> conditionLabel = Optional.empty();

        private Optional<Double> conditionIndex = Optional.empty();

        private Optional<String> valueMetric = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(DeliveredValueSummaryBucket other) {
            periodStart(other.getPeriodStart());
            periodEnd(other.getPeriodEnd());
            groupKey(other.getGroupKey());
            valueMetric(other.getValueMetric());
            conditionIndex(other.getConditionIndex());
            conditionLabel(other.getConditionLabel());
            unitTotal(other.getUnitTotal());
            monetaryTotal(other.getMonetaryTotal());
            signalCount(other.getSignalCount());
            return this;
        }

        /**
         * <p>Start of the bucket (inclusive), UTC. Clipped to the requested startDate when the window starts mid-period.</p>
         * <p>Start of the bucket (inclusive), UTC. Clipped to the requested startDate when the window starts mid-period.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("periodStart")
        public PeriodEndStage periodStart(@NotNull OffsetDateTime periodStart) {
            this.periodStart = Objects.requireNonNull(periodStart, "periodStart must not be null");
            return this;
        }

        /**
         * <p>Last instant of the bucket (inclusive), UTC. Clipped to the requested endDate when the window ends mid-period.</p>
         * <p>Last instant of the bucket (inclusive), UTC. Clipped to the requested endDate when the window ends mid-period.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("periodEnd")
        public GroupKeyStage periodEnd(@NotNull OffsetDateTime periodEnd) {
            this.periodEnd = Objects.requireNonNull(periodEnd, "periodEnd must not be null");
            return this;
        }

        /**
         * <p>The group this bucket belongs to. For valueMetric grouping, the metric's slug; an archived metric still appears when it delivered value in the window. For dimension grouping, the payload value of dimensionKey; a signal whose payload lacks the key, or holds it empty, groups under the empty string. For formulaCondition grouping, the metric's slug, a colon, and the band: the zero-based index of the condition that priced the rows, &quot;default&quot; where every condition missed, or &quot;none&quot; where the metric priced them through a formula that has no conditions. Read valueMetric and conditionIndex rather than parsing this.</p>
         * <p>The group this bucket belongs to. For valueMetric grouping, the metric's slug; an archived metric still appears when it delivered value in the window. For dimension grouping, the payload value of dimensionKey; a signal whose payload lacks the key, or holds it empty, groups under the empty string. For formulaCondition grouping, the metric's slug, a colon, and the band: the zero-based index of the condition that priced the rows, &quot;default&quot; where every condition missed, or &quot;none&quot; where the metric priced them through a formula that has no conditions. Read valueMetric and conditionIndex rather than parsing this.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("groupKey")
        public MonetaryTotalStage groupKey(@NotNull String groupKey) {
            this.groupKey = Objects.requireNonNull(groupKey, "groupKey must not be null");
            return this;
        }

        /**
         * <p>Money delivered in this bucket, in decimal units of the response currency (12.5 = $12.50), not cents.</p>
         * <p>Money delivered in this bucket, in decimal units of the response currency (12.5 = $12.50), not cents.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("monetaryTotal")
        public SignalCountStage monetaryTotal(double monetaryTotal) {
            this.monetaryTotal = monetaryTotal;
            return this;
        }

        /**
         * <p>Signals that contributed to this group in this bucket, counted by quantity — one signal reporting a batch of 100 counts as 100. For valueMetric grouping, a signal feeding two metrics counts under both groups. For dimension grouping, it counts once in its group.</p>
         * <p>Signals that contributed to this group in this bucket, counted by quantity — one signal reporting a batch of 100 counts as 100. For valueMetric grouping, a signal feeding two metrics counts under both groups. For dimension grouping, it counts once in its group.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("signalCount")
        public _FinalStage signalCount(double signalCount) {
            this.signalCount = signalCount;
            return this;
        }

        /**
         * <p>Value delivered in this bucket in the metric's own unit — minutes saved, tickets resolved — as GET /delivered-value states it. Present under valueMetric and formulaCondition grouping, where every row in a group belongs to one metric and so shares its unit. Absent under dimension grouping, which sums across metrics that need not measure the same thing.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage unitTotal(Double unitTotal) {
            this.unitTotal = Optional.ofNullable(unitTotal);
            return this;
        }

        /**
         * <p>Value delivered in this bucket in the metric's own unit — minutes saved, tickets resolved — as GET /delivered-value states it. Present under valueMetric and formulaCondition grouping, where every row in a group belongs to one metric and so shares its unit. Absent under dimension grouping, which sums across metrics that need not measure the same thing.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "unitTotal", nulls = Nulls.SKIP)
        public _FinalStage unitTotal(Optional<Double> unitTotal) {
            this.unitTotal = unitTotal;
            return this;
        }

        /**
         * <p>The condition that priced this band, as field, operator and value — a stable descriptor, not display copy. A metric priced through several formulas states the label they agree on; where they band differently at this index, no one condition describes the group and the label is absent.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage conditionLabel(String conditionLabel) {
            this.conditionLabel = Optional.ofNullable(conditionLabel);
            return this;
        }

        /**
         * <p>The condition that priced this band, as field, operator and value — a stable descriptor, not display copy. A metric priced through several formulas states the label they agree on; where they band differently at this index, no one condition describes the group and the label is absent.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "conditionLabel", nulls = Nulls.SKIP)
        public _FinalStage conditionLabel(Optional<String> conditionLabel) {
            this.conditionLabel = conditionLabel;
            return this;
        }

        /**
         * <p>Zero-based index of the formula condition that priced this band's rows, in the order the value model authors them. Present only under formulaCondition grouping, and only for a band that is a condition — absent for the &quot;default&quot; and &quot;none&quot; groups.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage conditionIndex(Double conditionIndex) {
            this.conditionIndex = Optional.ofNullable(conditionIndex);
            return this;
        }

        /**
         * <p>Zero-based index of the formula condition that priced this band's rows, in the order the value model authors them. Present only under formulaCondition grouping, and only for a band that is a condition — absent for the &quot;default&quot; and &quot;none&quot; groups.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "conditionIndex", nulls = Nulls.SKIP)
        public _FinalStage conditionIndex(Optional<Double> conditionIndex) {
            this.conditionIndex = conditionIndex;
            return this;
        }

        /**
         * <p>Slug of the metric this band belongs to. Present only under formulaCondition grouping.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage valueMetric(String valueMetric) {
            this.valueMetric = Optional.ofNullable(valueMetric);
            return this;
        }

        /**
         * <p>Slug of the metric this band belongs to. Present only under formulaCondition grouping.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "valueMetric", nulls = Nulls.SKIP)
        public _FinalStage valueMetric(Optional<String> valueMetric) {
            this.valueMetric = valueMetric;
            return this;
        }

        @java.lang.Override
        public DeliveredValueSummaryBucket build() {
            return new DeliveredValueSummaryBucket(
                    periodStart,
                    periodEnd,
                    groupKey,
                    valueMetric,
                    conditionIndex,
                    conditionLabel,
                    unitTotal,
                    monetaryTotal,
                    signalCount,
                    additionalProperties);
        }
    }
}
