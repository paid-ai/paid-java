/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.Nullable;
import com.paid.api.core.NullableNonemptyFilter;
import com.paid.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CustomView.Builder.class)
public final class CustomView {
    private final String displayId;

    private final String name;

    private final CustomViewStatus status;

    private final Optional<String> previewUrl;

    private final CustomViewScope scope;

    private final Map<String, Object> additionalProperties;

    private CustomView(
            String displayId,
            String name,
            CustomViewStatus status,
            Optional<String> previewUrl,
            CustomViewScope scope,
            Map<String, Object> additionalProperties) {
        this.displayId = displayId;
        this.name = name;
        this.status = status;
        this.previewUrl = previewUrl;
        this.scope = scope;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Stable public identifier used in preview/embed URLs.
     */
    @JsonProperty("displayId")
    public String getDisplayId() {
        return displayId;
    }

    @JsonProperty("name")
    public String getName() {
        return name;
    }

    /**
     * @return A view is DRAFT until explicitly published. For customer-scoped views, only PUBLISHED views are embeddable and served to end-customers; a DRAFT is not. For organization-scoped views, publishing is only a 'ready to use' marker — both DRAFT and PUBLISHED are visible to your organization's members in Paid (publishing does not change who in the org can see it), and neither is ever embeddable to an end-customer.
     */
    @JsonProperty("status")
    public CustomViewStatus getStatus() {
        return status;
    }

    /**
     * @return Absolute URL to open this view in the Paid app for preview. Surface it to the user after creating or publishing a view. Null when the app URL is not configured.
     */
    @JsonIgnore
    public Optional<String> getPreviewUrl() {
        if (previewUrl == null) {
            return Optional.empty();
        }
        return previewUrl;
    }

    /**
     * @return 'customer': data is scoped to one viewing customer and the view is embeddable per-customer. 'organization': data is org-wide and the view is internal-only.
     */
    @JsonProperty("scope")
    public CustomViewScope getScope() {
        return scope;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("previewUrl")
    private Optional<String> _getPreviewUrl() {
        return previewUrl;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CustomView && equalTo((CustomView) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CustomView other) {
        return displayId.equals(other.displayId)
                && name.equals(other.name)
                && status.equals(other.status)
                && previewUrl.equals(other.previewUrl)
                && scope.equals(other.scope);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.displayId, this.name, this.status, this.previewUrl, this.scope);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static DisplayIdStage builder() {
        return new Builder();
    }

    public interface DisplayIdStage {
        /**
         * <p>Stable public identifier used in preview/embed URLs.</p>
         */
        NameStage displayId(@NotNull String displayId);

        Builder from(CustomView other);
    }

    public interface NameStage {
        StatusStage name(@NotNull String name);
    }

    public interface StatusStage {
        /**
         * <p>A view is DRAFT until explicitly published. For customer-scoped views, only PUBLISHED views are embeddable and served to end-customers; a DRAFT is not. For organization-scoped views, publishing is only a 'ready to use' marker — both DRAFT and PUBLISHED are visible to your organization's members in Paid (publishing does not change who in the org can see it), and neither is ever embeddable to an end-customer.</p>
         */
        ScopeStage status(@NotNull CustomViewStatus status);
    }

    public interface ScopeStage {
        /**
         * <p>'customer': data is scoped to one viewing customer and the view is embeddable per-customer. 'organization': data is org-wide and the view is internal-only.</p>
         */
        _FinalStage scope(@NotNull CustomViewScope scope);
    }

    public interface _FinalStage {
        CustomView build();

        /**
         * <p>Absolute URL to open this view in the Paid app for preview. Surface it to the user after creating or publishing a view. Null when the app URL is not configured.</p>
         */
        _FinalStage previewUrl(Optional<String> previewUrl);

        _FinalStage previewUrl(String previewUrl);

        _FinalStage previewUrl(Nullable<String> previewUrl);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements DisplayIdStage, NameStage, StatusStage, ScopeStage, _FinalStage {
        private String displayId;

        private String name;

        private CustomViewStatus status;

        private CustomViewScope scope;

        private Optional<String> previewUrl = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CustomView other) {
            displayId(other.getDisplayId());
            name(other.getName());
            status(other.getStatus());
            previewUrl(other.getPreviewUrl());
            scope(other.getScope());
            return this;
        }

        /**
         * <p>Stable public identifier used in preview/embed URLs.</p>
         * <p>Stable public identifier used in preview/embed URLs.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("displayId")
        public NameStage displayId(@NotNull String displayId) {
            this.displayId = Objects.requireNonNull(displayId, "displayId must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("name")
        public StatusStage name(@NotNull String name) {
            this.name = Objects.requireNonNull(name, "name must not be null");
            return this;
        }

        /**
         * <p>A view is DRAFT until explicitly published. For customer-scoped views, only PUBLISHED views are embeddable and served to end-customers; a DRAFT is not. For organization-scoped views, publishing is only a 'ready to use' marker — both DRAFT and PUBLISHED are visible to your organization's members in Paid (publishing does not change who in the org can see it), and neither is ever embeddable to an end-customer.</p>
         * <p>A view is DRAFT until explicitly published. For customer-scoped views, only PUBLISHED views are embeddable and served to end-customers; a DRAFT is not. For organization-scoped views, publishing is only a 'ready to use' marker — both DRAFT and PUBLISHED are visible to your organization's members in Paid (publishing does not change who in the org can see it), and neither is ever embeddable to an end-customer.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public ScopeStage status(@NotNull CustomViewStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>'customer': data is scoped to one viewing customer and the view is embeddable per-customer. 'organization': data is org-wide and the view is internal-only.</p>
         * <p>'customer': data is scoped to one viewing customer and the view is embeddable per-customer. 'organization': data is org-wide and the view is internal-only.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("scope")
        public _FinalStage scope(@NotNull CustomViewScope scope) {
            this.scope = Objects.requireNonNull(scope, "scope must not be null");
            return this;
        }

        /**
         * <p>Absolute URL to open this view in the Paid app for preview. Surface it to the user after creating or publishing a view. Null when the app URL is not configured.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage previewUrl(Nullable<String> previewUrl) {
            if (previewUrl.isNull()) {
                this.previewUrl = null;
            } else if (previewUrl.isEmpty()) {
                this.previewUrl = Optional.empty();
            } else {
                this.previewUrl = Optional.of(previewUrl.get());
            }
            return this;
        }

        /**
         * <p>Absolute URL to open this view in the Paid app for preview. Surface it to the user after creating or publishing a view. Null when the app URL is not configured.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage previewUrl(String previewUrl) {
            this.previewUrl = Optional.ofNullable(previewUrl);
            return this;
        }

        /**
         * <p>Absolute URL to open this view in the Paid app for preview. Surface it to the user after creating or publishing a view. Null when the app URL is not configured.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "previewUrl", nulls = Nulls.SKIP)
        public _FinalStage previewUrl(Optional<String> previewUrl) {
            this.previewUrl = previewUrl;
            return this;
        }

        @java.lang.Override
        public CustomView build() {
            return new CustomView(displayId, name, status, previewUrl, scope, additionalProperties);
        }
    }
}
