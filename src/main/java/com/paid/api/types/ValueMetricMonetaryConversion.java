/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = ValueMetricMonetaryConversion.Builder.class)
public final class ValueMetricMonetaryConversion {
    private final double valuePerUnit;

    private final Optional<String> label;

    private final Map<String, Object> additionalProperties;

    private ValueMetricMonetaryConversion(
            double valuePerUnit, Optional<String> label, Map<String, Object> additionalProperties) {
        this.valuePerUnit = valuePerUnit;
        this.label = label;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Money earned or saved per one unit. Expands to 'unit_value * rate'.
     */
    @JsonProperty("valuePerUnit")
    public double getValuePerUnit() {
        return valuePerUnit;
    }

    /**
     * @return Name for the rate variable on customer-facing calculation strips. Defaults to 'rate'.
     */
    @JsonProperty("label")
    public Optional<String> getLabel() {
        return label;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ValueMetricMonetaryConversion && equalTo((ValueMetricMonetaryConversion) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(ValueMetricMonetaryConversion other) {
        return valuePerUnit == other.valuePerUnit && label.equals(other.label);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.valuePerUnit, this.label);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static ValuePerUnitStage builder() {
        return new Builder();
    }

    public interface ValuePerUnitStage {
        /**
         * <p>Money earned or saved per one unit. Expands to 'unit_value * rate'.</p>
         */
        _FinalStage valuePerUnit(double valuePerUnit);

        Builder from(ValueMetricMonetaryConversion other);
    }

    public interface _FinalStage {
        ValueMetricMonetaryConversion build();

        /**
         * <p>Name for the rate variable on customer-facing calculation strips. Defaults to 'rate'.</p>
         */
        _FinalStage label(Optional<String> label);

        _FinalStage label(String label);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements ValuePerUnitStage, _FinalStage {
        private double valuePerUnit;

        private Optional<String> label = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(ValueMetricMonetaryConversion other) {
            valuePerUnit(other.getValuePerUnit());
            label(other.getLabel());
            return this;
        }

        /**
         * <p>Money earned or saved per one unit. Expands to 'unit_value * rate'.</p>
         * <p>Money earned or saved per one unit. Expands to 'unit_value * rate'.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("valuePerUnit")
        public _FinalStage valuePerUnit(double valuePerUnit) {
            this.valuePerUnit = valuePerUnit;
            return this;
        }

        /**
         * <p>Name for the rate variable on customer-facing calculation strips. Defaults to 'rate'.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage label(String label) {
            this.label = Optional.ofNullable(label);
            return this;
        }

        /**
         * <p>Name for the rate variable on customer-facing calculation strips. Defaults to 'rate'.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "label", nulls = Nulls.SKIP)
        public _FinalStage label(Optional<String> label) {
            this.label = label;
            return this;
        }

        @java.lang.Override
        public ValueMetricMonetaryConversion build() {
            return new ValueMetricMonetaryConversion(valuePerUnit, label, additionalProperties);
        }
    }
}
