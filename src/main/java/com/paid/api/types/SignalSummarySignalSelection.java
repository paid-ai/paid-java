/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = SignalSummarySignalSelection.Builder.class)
public final class SignalSummarySignalSelection {
    private final String name;

    private final String group;

    private final Optional<String> slug;

    private final Map<String, Object> additionalProperties;

    private SignalSummarySignalSelection(
            String name, String group, Optional<String> slug, Map<String, Object> additionalProperties) {
        this.name = name;
        this.group = group;
        this.slug = slug;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Signal event name, matched exactly.
     */
    @JsonProperty("name")
    public String getName() {
        return name;
    }

    /**
     * @return Label shown in the file's first header row, above every column derived from this signal.
     */
    @JsonProperty("group")
    public String getGroup() {
        return group;
    }

    /**
     * @return Fragment used to build this signal's column names. Defaults to a slugified group.
     */
    @JsonProperty("slug")
    public Optional<String> getSlug() {
        return slug;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof SignalSummarySignalSelection && equalTo((SignalSummarySignalSelection) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(SignalSummarySignalSelection other) {
        return name.equals(other.name) && group.equals(other.group) && slug.equals(other.slug);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.name, this.group, this.slug);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static NameStage builder() {
        return new Builder();
    }

    public interface NameStage {
        /**
         * <p>Signal event name, matched exactly.</p>
         */
        GroupStage name(@NotNull String name);

        Builder from(SignalSummarySignalSelection other);
    }

    public interface GroupStage {
        /**
         * <p>Label shown in the file's first header row, above every column derived from this signal.</p>
         */
        _FinalStage group(@NotNull String group);
    }

    public interface _FinalStage {
        SignalSummarySignalSelection build();

        /**
         * <p>Fragment used to build this signal's column names. Defaults to a slugified group.</p>
         */
        _FinalStage slug(Optional<String> slug);

        _FinalStage slug(String slug);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements NameStage, GroupStage, _FinalStage {
        private String name;

        private String group;

        private Optional<String> slug = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(SignalSummarySignalSelection other) {
            name(other.getName());
            group(other.getGroup());
            slug(other.getSlug());
            return this;
        }

        /**
         * <p>Signal event name, matched exactly.</p>
         * <p>Signal event name, matched exactly.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("name")
        public GroupStage name(@NotNull String name) {
            this.name = Objects.requireNonNull(name, "name must not be null");
            return this;
        }

        /**
         * <p>Label shown in the file's first header row, above every column derived from this signal.</p>
         * <p>Label shown in the file's first header row, above every column derived from this signal.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("group")
        public _FinalStage group(@NotNull String group) {
            this.group = Objects.requireNonNull(group, "group must not be null");
            return this;
        }

        /**
         * <p>Fragment used to build this signal's column names. Defaults to a slugified group.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage slug(String slug) {
            this.slug = Optional.ofNullable(slug);
            return this;
        }

        /**
         * <p>Fragment used to build this signal's column names. Defaults to a slugified group.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "slug", nulls = Nulls.SKIP)
        public _FinalStage slug(Optional<String> slug) {
            this.slug = slug;
            return this;
        }

        @java.lang.Override
        public SignalSummarySignalSelection build() {
            return new SignalSummarySignalSelection(name, group, slug, additionalProperties);
        }
    }
}
