/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = RenderBundleEdit.Builder.class)
public final class RenderBundleEdit {
    private final String oldText;

    private final String newText;

    private final Optional<Boolean> replaceAll;

    private final Map<String, Object> additionalProperties;

    private RenderBundleEdit(
            String oldText, String newText, Optional<Boolean> replaceAll, Map<String, Object> additionalProperties) {
        this.oldText = oldText;
        this.newText = newText;
        this.replaceAll = replaceAll;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Exact text to find in the CURRENT stored bundle (fetch it via getCustomViewAuthoring first — it may have been hand-edited). Must match exactly once unless <code>replaceAll</code> is true; include enough surrounding context to make it unique.
     */
    @JsonProperty("oldText")
    public String getOldText() {
        return oldText;
    }

    /**
     * @return Replacement text. May be empty to delete the matched text.
     */
    @JsonProperty("newText")
    public String getNewText() {
        return newText;
    }

    /**
     * @return Replace every occurrence instead of requiring a unique match (e.g. renaming a variable or color). Default false.
     */
    @JsonProperty("replaceAll")
    public Optional<Boolean> getReplaceAll() {
        return replaceAll;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof RenderBundleEdit && equalTo((RenderBundleEdit) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(RenderBundleEdit other) {
        return oldText.equals(other.oldText) && newText.equals(other.newText) && replaceAll.equals(other.replaceAll);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.oldText, this.newText, this.replaceAll);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static OldTextStage builder() {
        return new Builder();
    }

    public interface OldTextStage {
        /**
         * <p>Exact text to find in the CURRENT stored bundle (fetch it via getCustomViewAuthoring first — it may have been hand-edited). Must match exactly once unless <code>replaceAll</code> is true; include enough surrounding context to make it unique.</p>
         */
        NewTextStage oldText(@NotNull String oldText);

        Builder from(RenderBundleEdit other);
    }

    public interface NewTextStage {
        /**
         * <p>Replacement text. May be empty to delete the matched text.</p>
         */
        _FinalStage newText(@NotNull String newText);
    }

    public interface _FinalStage {
        RenderBundleEdit build();

        /**
         * <p>Replace every occurrence instead of requiring a unique match (e.g. renaming a variable or color). Default false.</p>
         */
        _FinalStage replaceAll(Optional<Boolean> replaceAll);

        _FinalStage replaceAll(Boolean replaceAll);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements OldTextStage, NewTextStage, _FinalStage {
        private String oldText;

        private String newText;

        private Optional<Boolean> replaceAll = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(RenderBundleEdit other) {
            oldText(other.getOldText());
            newText(other.getNewText());
            replaceAll(other.getReplaceAll());
            return this;
        }

        /**
         * <p>Exact text to find in the CURRENT stored bundle (fetch it via getCustomViewAuthoring first — it may have been hand-edited). Must match exactly once unless <code>replaceAll</code> is true; include enough surrounding context to make it unique.</p>
         * <p>Exact text to find in the CURRENT stored bundle (fetch it via getCustomViewAuthoring first — it may have been hand-edited). Must match exactly once unless <code>replaceAll</code> is true; include enough surrounding context to make it unique.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("oldText")
        public NewTextStage oldText(@NotNull String oldText) {
            this.oldText = Objects.requireNonNull(oldText, "oldText must not be null");
            return this;
        }

        /**
         * <p>Replacement text. May be empty to delete the matched text.</p>
         * <p>Replacement text. May be empty to delete the matched text.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("newText")
        public _FinalStage newText(@NotNull String newText) {
            this.newText = Objects.requireNonNull(newText, "newText must not be null");
            return this;
        }

        /**
         * <p>Replace every occurrence instead of requiring a unique match (e.g. renaming a variable or color). Default false.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage replaceAll(Boolean replaceAll) {
            this.replaceAll = Optional.ofNullable(replaceAll);
            return this;
        }

        /**
         * <p>Replace every occurrence instead of requiring a unique match (e.g. renaming a variable or color). Default false.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "replaceAll", nulls = Nulls.SKIP)
        public _FinalStage replaceAll(Optional<Boolean> replaceAll) {
            this.replaceAll = replaceAll;
            return this;
        }

        @java.lang.Override
        public RenderBundleEdit build() {
            return new RenderBundleEdit(oldText, newText, replaceAll, additionalProperties);
        }
    }
}
