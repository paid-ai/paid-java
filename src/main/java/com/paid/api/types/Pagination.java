/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = Pagination.Builder.class)
public final class Pagination {
    private final int limit;

    private final int offset;

    private final int total;

    private final boolean hasMore;

    private final Map<String, Object> additionalProperties;

    private Pagination(int limit, int offset, int total, boolean hasMore, Map<String, Object> additionalProperties) {
        this.limit = limit;
        this.offset = offset;
        this.total = total;
        this.hasMore = hasMore;
        this.additionalProperties = additionalProperties;
    }

    @JsonProperty("limit")
    public int getLimit() {
        return limit;
    }

    @JsonProperty("offset")
    public int getOffset() {
        return offset;
    }

    @JsonProperty("total")
    public int getTotal() {
        return total;
    }

    @JsonProperty("hasMore")
    public boolean getHasMore() {
        return hasMore;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof Pagination && equalTo((Pagination) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(Pagination other) {
        return limit == other.limit && offset == other.offset && total == other.total && hasMore == other.hasMore;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.limit, this.offset, this.total, this.hasMore);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static LimitStage builder() {
        return new Builder();
    }

    public interface LimitStage {
        OffsetStage limit(int limit);

        Builder from(Pagination other);
    }

    public interface OffsetStage {
        TotalStage offset(int offset);
    }

    public interface TotalStage {
        HasMoreStage total(int total);
    }

    public interface HasMoreStage {
        _FinalStage hasMore(boolean hasMore);
    }

    public interface _FinalStage {
        Pagination build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements LimitStage, OffsetStage, TotalStage, HasMoreStage, _FinalStage {
        private int limit;

        private int offset;

        private int total;

        private boolean hasMore;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(Pagination other) {
            limit(other.getLimit());
            offset(other.getOffset());
            total(other.getTotal());
            hasMore(other.getHasMore());
            return this;
        }

        @java.lang.Override
        @JsonSetter("limit")
        public OffsetStage limit(int limit) {
            this.limit = limit;
            return this;
        }

        @java.lang.Override
        @JsonSetter("offset")
        public TotalStage offset(int offset) {
            this.offset = offset;
            return this;
        }

        @java.lang.Override
        @JsonSetter("total")
        public HasMoreStage total(int total) {
            this.total = total;
            return this;
        }

        @java.lang.Override
        @JsonSetter("hasMore")
        public _FinalStage hasMore(boolean hasMore) {
            this.hasMore = hasMore;
            return this;
        }

        @java.lang.Override
        public Pagination build() {
            return new Pagination(limit, offset, total, hasMore, additionalProperties);
        }
    }
}
