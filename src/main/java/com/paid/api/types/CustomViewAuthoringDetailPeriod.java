/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CustomViewAuthoringDetailPeriod.Builder.class)
public final class CustomViewAuthoringDetailPeriod {
    private final CustomViewAuthoringDetailPeriodKind kind;

    private final Optional<CustomViewAuthoringDetailPeriodUnit> unit;

    private final Optional<Integer> amount;

    private final Optional<String> start;

    private final Optional<String> end;

    private final Map<String, Object> additionalProperties;

    private CustomViewAuthoringDetailPeriod(
            CustomViewAuthoringDetailPeriodKind kind,
            Optional<CustomViewAuthoringDetailPeriodUnit> unit,
            Optional<Integer> amount,
            Optional<String> start,
            Optional<String> end,
            Map<String, Object> additionalProperties) {
        this.kind = kind;
        this.unit = unit;
        this.amount = amount;
        this.start = start;
        this.end = end;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return 'relative' = a rolling window ending today (set <code>unit</code> + <code>amount</code>); 'absolute' = a fixed calendar range (set <code>start</code> + <code>end</code>).
     */
    @JsonProperty("kind")
    public CustomViewAuthoringDetailPeriodKind getKind() {
        return kind;
    }

    /**
     * @return relative only: the unit of the rolling window.
     */
    @JsonProperty("unit")
    public Optional<CustomViewAuthoringDetailPeriodUnit> getUnit() {
        return unit;
    }

    /**
     * @return relative only: how many units back from today (e.g. unit 'day', amount 30 = last 30 days).
     */
    @JsonProperty("amount")
    public Optional<Integer> getAmount() {
        return amount;
    }

    /**
     * @return absolute only: inclusive start date (YYYY-MM-DD).
     */
    @JsonProperty("start")
    public Optional<String> getStart() {
        return start;
    }

    /**
     * @return absolute only: inclusive end date (YYYY-MM-DD).
     */
    @JsonProperty("end")
    public Optional<String> getEnd() {
        return end;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CustomViewAuthoringDetailPeriod && equalTo((CustomViewAuthoringDetailPeriod) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CustomViewAuthoringDetailPeriod other) {
        return kind.equals(other.kind)
                && unit.equals(other.unit)
                && amount.equals(other.amount)
                && start.equals(other.start)
                && end.equals(other.end);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.kind, this.unit, this.amount, this.start, this.end);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static KindStage builder() {
        return new Builder();
    }

    public interface KindStage {
        /**
         * <p>'relative' = a rolling window ending today (set <code>unit</code> + <code>amount</code>); 'absolute' = a fixed calendar range (set <code>start</code> + <code>end</code>).</p>
         */
        _FinalStage kind(@NotNull CustomViewAuthoringDetailPeriodKind kind);

        Builder from(CustomViewAuthoringDetailPeriod other);
    }

    public interface _FinalStage {
        CustomViewAuthoringDetailPeriod build();

        /**
         * <p>relative only: the unit of the rolling window.</p>
         */
        _FinalStage unit(Optional<CustomViewAuthoringDetailPeriodUnit> unit);

        _FinalStage unit(CustomViewAuthoringDetailPeriodUnit unit);

        /**
         * <p>relative only: how many units back from today (e.g. unit 'day', amount 30 = last 30 days).</p>
         */
        _FinalStage amount(Optional<Integer> amount);

        _FinalStage amount(Integer amount);

        /**
         * <p>absolute only: inclusive start date (YYYY-MM-DD).</p>
         */
        _FinalStage start(Optional<String> start);

        _FinalStage start(String start);

        /**
         * <p>absolute only: inclusive end date (YYYY-MM-DD).</p>
         */
        _FinalStage end(Optional<String> end);

        _FinalStage end(String end);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements KindStage, _FinalStage {
        private CustomViewAuthoringDetailPeriodKind kind;

        private Optional<String> end = Optional.empty();

        private Optional<String> start = Optional.empty();

        private Optional<Integer> amount = Optional.empty();

        private Optional<CustomViewAuthoringDetailPeriodUnit> unit = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CustomViewAuthoringDetailPeriod other) {
            kind(other.getKind());
            unit(other.getUnit());
            amount(other.getAmount());
            start(other.getStart());
            end(other.getEnd());
            return this;
        }

        /**
         * <p>'relative' = a rolling window ending today (set <code>unit</code> + <code>amount</code>); 'absolute' = a fixed calendar range (set <code>start</code> + <code>end</code>).</p>
         * <p>'relative' = a rolling window ending today (set <code>unit</code> + <code>amount</code>); 'absolute' = a fixed calendar range (set <code>start</code> + <code>end</code>).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("kind")
        public _FinalStage kind(@NotNull CustomViewAuthoringDetailPeriodKind kind) {
            this.kind = Objects.requireNonNull(kind, "kind must not be null");
            return this;
        }

        /**
         * <p>absolute only: inclusive end date (YYYY-MM-DD).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage end(String end) {
            this.end = Optional.ofNullable(end);
            return this;
        }

        /**
         * <p>absolute only: inclusive end date (YYYY-MM-DD).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "end", nulls = Nulls.SKIP)
        public _FinalStage end(Optional<String> end) {
            this.end = end;
            return this;
        }

        /**
         * <p>absolute only: inclusive start date (YYYY-MM-DD).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage start(String start) {
            this.start = Optional.ofNullable(start);
            return this;
        }

        /**
         * <p>absolute only: inclusive start date (YYYY-MM-DD).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "start", nulls = Nulls.SKIP)
        public _FinalStage start(Optional<String> start) {
            this.start = start;
            return this;
        }

        /**
         * <p>relative only: how many units back from today (e.g. unit 'day', amount 30 = last 30 days).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage amount(Integer amount) {
            this.amount = Optional.ofNullable(amount);
            return this;
        }

        /**
         * <p>relative only: how many units back from today (e.g. unit 'day', amount 30 = last 30 days).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "amount", nulls = Nulls.SKIP)
        public _FinalStage amount(Optional<Integer> amount) {
            this.amount = amount;
            return this;
        }

        /**
         * <p>relative only: the unit of the rolling window.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage unit(CustomViewAuthoringDetailPeriodUnit unit) {
            this.unit = Optional.ofNullable(unit);
            return this;
        }

        /**
         * <p>relative only: the unit of the rolling window.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "unit", nulls = Nulls.SKIP)
        public _FinalStage unit(Optional<CustomViewAuthoringDetailPeriodUnit> unit) {
            this.unit = unit;
            return this;
        }

        @java.lang.Override
        public CustomViewAuthoringDetailPeriod build() {
            return new CustomViewAuthoringDetailPeriod(kind, unit, amount, start, end, additionalProperties);
        }
    }
}
