/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.Nullable;
import com.paid.api.core.NullableNonemptyFilter;
import com.paid.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CreditTransaction.Builder.class)
public final class CreditTransaction {
    private final String id;

    private final CreditTransactionCustomer customer;

    private final CreditTransactionCurrency creditCurrency;

    private final CreditTransactionType type;

    private final String amount;

    private final String originCode;

    private final Optional<String> eventName;

    private final Optional<String> signalId;

    private final Optional<String> orderId;

    private final Optional<CreditTransactionProduct> product;

    private final OffsetDateTime createdAt;

    private final Map<String, Object> additionalProperties;

    private CreditTransaction(
            String id,
            CreditTransactionCustomer customer,
            CreditTransactionCurrency creditCurrency,
            CreditTransactionType type,
            String amount,
            String originCode,
            Optional<String> eventName,
            Optional<String> signalId,
            Optional<String> orderId,
            Optional<CreditTransactionProduct> product,
            OffsetDateTime createdAt,
            Map<String, Object> additionalProperties) {
        this.id = id;
        this.customer = customer;
        this.creditCurrency = creditCurrency;
        this.type = type;
        this.amount = amount;
        this.originCode = originCode;
        this.eventName = eventName;
        this.signalId = signalId;
        this.orderId = orderId;
        this.product = product;
        this.createdAt = createdAt;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Unique identifier for this credit transaction.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    @JsonProperty("customer")
    public CreditTransactionCustomer getCustomer() {
        return customer;
    }

    @JsonProperty("creditCurrency")
    public CreditTransactionCurrency getCreditCurrency() {
        return creditCurrency;
    }

    /**
     * @return Transaction type: <code>grant</code> adds credits, <code>spend</code> consumes them (negative amount), <code>pendingGrant</code> records credits awaiting payment.
     */
    @JsonProperty("type")
    public CreditTransactionType getType() {
        return type;
    }

    /**
     * @return Exact signed transaction amount in credits, as a decimal string: positive for grants, negative for spends. Exact at any magnitude, and at the full 6 decimal places.
     */
    @JsonProperty("amount")
    public String getAmount() {
        return amount;
    }

    /**
     * @return Origin of the credit pool this transaction touched (e.g. <code>purchased</code>, or a custom label). <code>unknown</code> when the transaction is not linked to a pool with an origin.
     */
    @JsonProperty("originCode")
    public String getOriginCode() {
        return originCode;
    }

    /**
     * @return Usage event that produced this spend. Null for grants and non-signal spends.
     */
    @JsonIgnore
    public Optional<String> getEventName() {
        if (eventName == null) {
            return Optional.empty();
        }
        return eventName;
    }

    /**
     * @return ID of the usage signal that produced this spend, resolvable via <code>GET /api/v2/signals/{id}</code> (including the signal's data payload). Null for grants and non-signal spends.
     */
    @JsonIgnore
    public Optional<String> getSignalId() {
        if (signalId == null) {
            return Optional.empty();
        }
        return signalId;
    }

    /**
     * @return Paid order display ID this transaction is linked to, if any.
     */
    @JsonIgnore
    public Optional<String> getOrderId() {
        if (orderId == null) {
            return Optional.empty();
        }
        return orderId;
    }

    @JsonIgnore
    public Optional<CreditTransactionProduct> getProduct() {
        if (product == null) {
            return Optional.empty();
        }
        return product;
    }

    /**
     * @return When the originating signal was recorded, or when the transaction was recorded if it has no signal.
     */
    @JsonProperty("createdAt")
    public OffsetDateTime getCreatedAt() {
        return createdAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("eventName")
    private Optional<String> _getEventName() {
        return eventName;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("signalId")
    private Optional<String> _getSignalId() {
        return signalId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("orderId")
    private Optional<String> _getOrderId() {
        return orderId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("product")
    private Optional<CreditTransactionProduct> _getProduct() {
        return product;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreditTransaction && equalTo((CreditTransaction) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CreditTransaction other) {
        return id.equals(other.id)
                && customer.equals(other.customer)
                && creditCurrency.equals(other.creditCurrency)
                && type.equals(other.type)
                && amount.equals(other.amount)
                && originCode.equals(other.originCode)
                && eventName.equals(other.eventName)
                && signalId.equals(other.signalId)
                && orderId.equals(other.orderId)
                && product.equals(other.product)
                && createdAt.equals(other.createdAt);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.id,
                this.customer,
                this.creditCurrency,
                this.type,
                this.amount,
                this.originCode,
                this.eventName,
                this.signalId,
                this.orderId,
                this.product,
                this.createdAt);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>Unique identifier for this credit transaction.</p>
         */
        CustomerStage id(@NotNull String id);

        Builder from(CreditTransaction other);
    }

    public interface CustomerStage {
        CreditCurrencyStage customer(@NotNull CreditTransactionCustomer customer);
    }

    public interface CreditCurrencyStage {
        TypeStage creditCurrency(@NotNull CreditTransactionCurrency creditCurrency);
    }

    public interface TypeStage {
        /**
         * <p>Transaction type: <code>grant</code> adds credits, <code>spend</code> consumes them (negative amount), <code>pendingGrant</code> records credits awaiting payment.</p>
         */
        AmountStage type(@NotNull CreditTransactionType type);
    }

    public interface AmountStage {
        /**
         * <p>Exact signed transaction amount in credits, as a decimal string: positive for grants, negative for spends. Exact at any magnitude, and at the full 6 decimal places.</p>
         */
        OriginCodeStage amount(@NotNull String amount);
    }

    public interface OriginCodeStage {
        /**
         * <p>Origin of the credit pool this transaction touched (e.g. <code>purchased</code>, or a custom label). <code>unknown</code> when the transaction is not linked to a pool with an origin.</p>
         */
        CreatedAtStage originCode(@NotNull String originCode);
    }

    public interface CreatedAtStage {
        /**
         * <p>When the originating signal was recorded, or when the transaction was recorded if it has no signal.</p>
         */
        _FinalStage createdAt(@NotNull OffsetDateTime createdAt);
    }

    public interface _FinalStage {
        CreditTransaction build();

        /**
         * <p>Usage event that produced this spend. Null for grants and non-signal spends.</p>
         */
        _FinalStage eventName(Optional<String> eventName);

        _FinalStage eventName(String eventName);

        _FinalStage eventName(Nullable<String> eventName);

        /**
         * <p>ID of the usage signal that produced this spend, resolvable via <code>GET /api/v2/signals/{id}</code> (including the signal's data payload). Null for grants and non-signal spends.</p>
         */
        _FinalStage signalId(Optional<String> signalId);

        _FinalStage signalId(String signalId);

        _FinalStage signalId(Nullable<String> signalId);

        /**
         * <p>Paid order display ID this transaction is linked to, if any.</p>
         */
        _FinalStage orderId(Optional<String> orderId);

        _FinalStage orderId(String orderId);

        _FinalStage orderId(Nullable<String> orderId);

        _FinalStage product(Optional<CreditTransactionProduct> product);

        _FinalStage product(CreditTransactionProduct product);

        _FinalStage product(Nullable<CreditTransactionProduct> product);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements IdStage,
                    CustomerStage,
                    CreditCurrencyStage,
                    TypeStage,
                    AmountStage,
                    OriginCodeStage,
                    CreatedAtStage,
                    _FinalStage {
        private String id;

        private CreditTransactionCustomer customer;

        private CreditTransactionCurrency creditCurrency;

        private CreditTransactionType type;

        private String amount;

        private String originCode;

        private OffsetDateTime createdAt;

        private Optional<CreditTransactionProduct> product = Optional.empty();

        private Optional<String> orderId = Optional.empty();

        private Optional<String> signalId = Optional.empty();

        private Optional<String> eventName = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CreditTransaction other) {
            id(other.getId());
            customer(other.getCustomer());
            creditCurrency(other.getCreditCurrency());
            type(other.getType());
            amount(other.getAmount());
            originCode(other.getOriginCode());
            eventName(other.getEventName());
            signalId(other.getSignalId());
            orderId(other.getOrderId());
            product(other.getProduct());
            createdAt(other.getCreatedAt());
            return this;
        }

        /**
         * <p>Unique identifier for this credit transaction.</p>
         * <p>Unique identifier for this credit transaction.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public CustomerStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("customer")
        public CreditCurrencyStage customer(@NotNull CreditTransactionCustomer customer) {
            this.customer = Objects.requireNonNull(customer, "customer must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("creditCurrency")
        public TypeStage creditCurrency(@NotNull CreditTransactionCurrency creditCurrency) {
            this.creditCurrency = Objects.requireNonNull(creditCurrency, "creditCurrency must not be null");
            return this;
        }

        /**
         * <p>Transaction type: <code>grant</code> adds credits, <code>spend</code> consumes them (negative amount), <code>pendingGrant</code> records credits awaiting payment.</p>
         * <p>Transaction type: <code>grant</code> adds credits, <code>spend</code> consumes them (negative amount), <code>pendingGrant</code> records credits awaiting payment.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("type")
        public AmountStage type(@NotNull CreditTransactionType type) {
            this.type = Objects.requireNonNull(type, "type must not be null");
            return this;
        }

        /**
         * <p>Exact signed transaction amount in credits, as a decimal string: positive for grants, negative for spends. Exact at any magnitude, and at the full 6 decimal places.</p>
         * <p>Exact signed transaction amount in credits, as a decimal string: positive for grants, negative for spends. Exact at any magnitude, and at the full 6 decimal places.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("amount")
        public OriginCodeStage amount(@NotNull String amount) {
            this.amount = Objects.requireNonNull(amount, "amount must not be null");
            return this;
        }

        /**
         * <p>Origin of the credit pool this transaction touched (e.g. <code>purchased</code>, or a custom label). <code>unknown</code> when the transaction is not linked to a pool with an origin.</p>
         * <p>Origin of the credit pool this transaction touched (e.g. <code>purchased</code>, or a custom label). <code>unknown</code> when the transaction is not linked to a pool with an origin.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("originCode")
        public CreatedAtStage originCode(@NotNull String originCode) {
            this.originCode = Objects.requireNonNull(originCode, "originCode must not be null");
            return this;
        }

        /**
         * <p>When the originating signal was recorded, or when the transaction was recorded if it has no signal.</p>
         * <p>When the originating signal was recorded, or when the transaction was recorded if it has no signal.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("createdAt")
        public _FinalStage createdAt(@NotNull OffsetDateTime createdAt) {
            this.createdAt = Objects.requireNonNull(createdAt, "createdAt must not be null");
            return this;
        }

        @java.lang.Override
        public _FinalStage product(Nullable<CreditTransactionProduct> product) {
            if (product.isNull()) {
                this.product = null;
            } else if (product.isEmpty()) {
                this.product = Optional.empty();
            } else {
                this.product = Optional.of(product.get());
            }
            return this;
        }

        @java.lang.Override
        public _FinalStage product(CreditTransactionProduct product) {
            this.product = Optional.ofNullable(product);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "product", nulls = Nulls.SKIP)
        public _FinalStage product(Optional<CreditTransactionProduct> product) {
            this.product = product;
            return this;
        }

        /**
         * <p>Paid order display ID this transaction is linked to, if any.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage orderId(Nullable<String> orderId) {
            if (orderId.isNull()) {
                this.orderId = null;
            } else if (orderId.isEmpty()) {
                this.orderId = Optional.empty();
            } else {
                this.orderId = Optional.of(orderId.get());
            }
            return this;
        }

        /**
         * <p>Paid order display ID this transaction is linked to, if any.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage orderId(String orderId) {
            this.orderId = Optional.ofNullable(orderId);
            return this;
        }

        /**
         * <p>Paid order display ID this transaction is linked to, if any.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "orderId", nulls = Nulls.SKIP)
        public _FinalStage orderId(Optional<String> orderId) {
            this.orderId = orderId;
            return this;
        }

        /**
         * <p>ID of the usage signal that produced this spend, resolvable via <code>GET /api/v2/signals/{id}</code> (including the signal's data payload). Null for grants and non-signal spends.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage signalId(Nullable<String> signalId) {
            if (signalId.isNull()) {
                this.signalId = null;
            } else if (signalId.isEmpty()) {
                this.signalId = Optional.empty();
            } else {
                this.signalId = Optional.of(signalId.get());
            }
            return this;
        }

        /**
         * <p>ID of the usage signal that produced this spend, resolvable via <code>GET /api/v2/signals/{id}</code> (including the signal's data payload). Null for grants and non-signal spends.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage signalId(String signalId) {
            this.signalId = Optional.ofNullable(signalId);
            return this;
        }

        /**
         * <p>ID of the usage signal that produced this spend, resolvable via <code>GET /api/v2/signals/{id}</code> (including the signal's data payload). Null for grants and non-signal spends.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "signalId", nulls = Nulls.SKIP)
        public _FinalStage signalId(Optional<String> signalId) {
            this.signalId = signalId;
            return this;
        }

        /**
         * <p>Usage event that produced this spend. Null for grants and non-signal spends.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage eventName(Nullable<String> eventName) {
            if (eventName.isNull()) {
                this.eventName = null;
            } else if (eventName.isEmpty()) {
                this.eventName = Optional.empty();
            } else {
                this.eventName = Optional.of(eventName.get());
            }
            return this;
        }

        /**
         * <p>Usage event that produced this spend. Null for grants and non-signal spends.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage eventName(String eventName) {
            this.eventName = Optional.ofNullable(eventName);
            return this;
        }

        /**
         * <p>Usage event that produced this spend. Null for grants and non-signal spends.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "eventName", nulls = Nulls.SKIP)
        public _FinalStage eventName(Optional<String> eventName) {
            this.eventName = eventName;
            return this;
        }

        @java.lang.Override
        public CreditTransaction build() {
            return new CreditTransaction(
                    id,
                    customer,
                    creditCurrency,
                    type,
                    amount,
                    originCode,
                    eventName,
                    signalId,
                    orderId,
                    product,
                    createdAt,
                    additionalProperties);
        }
    }
}
