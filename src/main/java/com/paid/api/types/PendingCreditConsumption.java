/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.Nullable;
import com.paid.api.core.NullableNonemptyFilter;
import com.paid.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = PendingCreditConsumption.Builder.class)
public final class PendingCreditConsumption {
    private final int credits;

    private final String creditsDecimal;

    private final Optional<PendingCreditConsumptionCurrency> creditCurrency;

    private final Optional<String> orderId;

    private final Optional<String> eventName;

    private final OffsetDateTime occurredAt;

    private final OffsetDateTime recordedAt;

    private final Map<String, Object> additionalProperties;

    private PendingCreditConsumption(
            int credits,
            String creditsDecimal,
            Optional<PendingCreditConsumptionCurrency> creditCurrency,
            Optional<String> orderId,
            Optional<String> eventName,
            OffsetDateTime occurredAt,
            OffsetDateTime recordedAt,
            Map<String, Object> additionalProperties) {
        this.credits = credits;
        this.creditsDecimal = creditsDecimal;
        this.creditCurrency = creditCurrency;
        this.orderId = orderId;
        this.eventName = eventName;
        this.occurredAt = occurredAt;
        this.recordedAt = recordedAt;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Whole-credit projection of the credits consumed, truncated toward zero. May be 0 for a sub-unit consumption. Exact for values within ±2^53; see creditsDecimal for the exact value at any magnitude. This is not a monetary amount.
     */
    @JsonProperty("credits")
    public int getCredits() {
        return credits;
    }

    /**
     * @return Exact number of credits consumed, as a decimal string. Exact at any magnitude, and at the full 6 decimal places.
     */
    @JsonProperty("creditsDecimal")
    public String getCreditsDecimal() {
        return creditsDecimal;
    }

    @JsonIgnore
    public Optional<PendingCreditConsumptionCurrency> getCreditCurrency() {
        if (creditCurrency == null) {
            return Optional.empty();
        }
        return creditCurrency;
    }

    /**
     * @return Paid order display ID the consumption belongs to, if any.
     */
    @JsonIgnore
    public Optional<String> getOrderId() {
        if (orderId == null) {
            return Optional.empty();
        }
        return orderId;
    }

    /**
     * @return Usage event that produced the consumption.
     */
    @JsonIgnore
    public Optional<String> getEventName() {
        if (eventName == null) {
            return Optional.empty();
        }
        return eventName;
    }

    /**
     * @return When the usage actually happened.
     */
    @JsonProperty("occurredAt")
    public OffsetDateTime getOccurredAt() {
        return occurredAt;
    }

    /**
     * @return When the consumption was recorded as pending.
     */
    @JsonProperty("recordedAt")
    public OffsetDateTime getRecordedAt() {
        return recordedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("creditCurrency")
    private Optional<PendingCreditConsumptionCurrency> _getCreditCurrency() {
        return creditCurrency;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("orderId")
    private Optional<String> _getOrderId() {
        return orderId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("eventName")
    private Optional<String> _getEventName() {
        return eventName;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PendingCreditConsumption && equalTo((PendingCreditConsumption) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(PendingCreditConsumption other) {
        return credits == other.credits
                && creditsDecimal.equals(other.creditsDecimal)
                && creditCurrency.equals(other.creditCurrency)
                && orderId.equals(other.orderId)
                && eventName.equals(other.eventName)
                && occurredAt.equals(other.occurredAt)
                && recordedAt.equals(other.recordedAt);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.credits,
                this.creditsDecimal,
                this.creditCurrency,
                this.orderId,
                this.eventName,
                this.occurredAt,
                this.recordedAt);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static CreditsStage builder() {
        return new Builder();
    }

    public interface CreditsStage {
        /**
         * <p>Whole-credit projection of the credits consumed, truncated toward zero. May be 0 for a sub-unit consumption. Exact for values within ±2^53; see creditsDecimal for the exact value at any magnitude. This is not a monetary amount.</p>
         */
        CreditsDecimalStage credits(int credits);

        Builder from(PendingCreditConsumption other);
    }

    public interface CreditsDecimalStage {
        /**
         * <p>Exact number of credits consumed, as a decimal string. Exact at any magnitude, and at the full 6 decimal places.</p>
         */
        OccurredAtStage creditsDecimal(@NotNull String creditsDecimal);
    }

    public interface OccurredAtStage {
        /**
         * <p>When the usage actually happened.</p>
         */
        RecordedAtStage occurredAt(@NotNull OffsetDateTime occurredAt);
    }

    public interface RecordedAtStage {
        /**
         * <p>When the consumption was recorded as pending.</p>
         */
        _FinalStage recordedAt(@NotNull OffsetDateTime recordedAt);
    }

    public interface _FinalStage {
        PendingCreditConsumption build();

        _FinalStage creditCurrency(Optional<PendingCreditConsumptionCurrency> creditCurrency);

        _FinalStage creditCurrency(PendingCreditConsumptionCurrency creditCurrency);

        _FinalStage creditCurrency(Nullable<PendingCreditConsumptionCurrency> creditCurrency);

        /**
         * <p>Paid order display ID the consumption belongs to, if any.</p>
         */
        _FinalStage orderId(Optional<String> orderId);

        _FinalStage orderId(String orderId);

        _FinalStage orderId(Nullable<String> orderId);

        /**
         * <p>Usage event that produced the consumption.</p>
         */
        _FinalStage eventName(Optional<String> eventName);

        _FinalStage eventName(String eventName);

        _FinalStage eventName(Nullable<String> eventName);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements CreditsStage, CreditsDecimalStage, OccurredAtStage, RecordedAtStage, _FinalStage {
        private int credits;

        private String creditsDecimal;

        private OffsetDateTime occurredAt;

        private OffsetDateTime recordedAt;

        private Optional<String> eventName = Optional.empty();

        private Optional<String> orderId = Optional.empty();

        private Optional<PendingCreditConsumptionCurrency> creditCurrency = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(PendingCreditConsumption other) {
            credits(other.getCredits());
            creditsDecimal(other.getCreditsDecimal());
            creditCurrency(other.getCreditCurrency());
            orderId(other.getOrderId());
            eventName(other.getEventName());
            occurredAt(other.getOccurredAt());
            recordedAt(other.getRecordedAt());
            return this;
        }

        /**
         * <p>Whole-credit projection of the credits consumed, truncated toward zero. May be 0 for a sub-unit consumption. Exact for values within ±2^53; see creditsDecimal for the exact value at any magnitude. This is not a monetary amount.</p>
         * <p>Whole-credit projection of the credits consumed, truncated toward zero. May be 0 for a sub-unit consumption. Exact for values within ±2^53; see creditsDecimal for the exact value at any magnitude. This is not a monetary amount.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("credits")
        public CreditsDecimalStage credits(int credits) {
            this.credits = credits;
            return this;
        }

        /**
         * <p>Exact number of credits consumed, as a decimal string. Exact at any magnitude, and at the full 6 decimal places.</p>
         * <p>Exact number of credits consumed, as a decimal string. Exact at any magnitude, and at the full 6 decimal places.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("creditsDecimal")
        public OccurredAtStage creditsDecimal(@NotNull String creditsDecimal) {
            this.creditsDecimal = Objects.requireNonNull(creditsDecimal, "creditsDecimal must not be null");
            return this;
        }

        /**
         * <p>When the usage actually happened.</p>
         * <p>When the usage actually happened.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("occurredAt")
        public RecordedAtStage occurredAt(@NotNull OffsetDateTime occurredAt) {
            this.occurredAt = Objects.requireNonNull(occurredAt, "occurredAt must not be null");
            return this;
        }

        /**
         * <p>When the consumption was recorded as pending.</p>
         * <p>When the consumption was recorded as pending.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("recordedAt")
        public _FinalStage recordedAt(@NotNull OffsetDateTime recordedAt) {
            this.recordedAt = Objects.requireNonNull(recordedAt, "recordedAt must not be null");
            return this;
        }

        /**
         * <p>Usage event that produced the consumption.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage eventName(Nullable<String> eventName) {
            if (eventName.isNull()) {
                this.eventName = null;
            } else if (eventName.isEmpty()) {
                this.eventName = Optional.empty();
            } else {
                this.eventName = Optional.of(eventName.get());
            }
            return this;
        }

        /**
         * <p>Usage event that produced the consumption.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage eventName(String eventName) {
            this.eventName = Optional.ofNullable(eventName);
            return this;
        }

        /**
         * <p>Usage event that produced the consumption.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "eventName", nulls = Nulls.SKIP)
        public _FinalStage eventName(Optional<String> eventName) {
            this.eventName = eventName;
            return this;
        }

        /**
         * <p>Paid order display ID the consumption belongs to, if any.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage orderId(Nullable<String> orderId) {
            if (orderId.isNull()) {
                this.orderId = null;
            } else if (orderId.isEmpty()) {
                this.orderId = Optional.empty();
            } else {
                this.orderId = Optional.of(orderId.get());
            }
            return this;
        }

        /**
         * <p>Paid order display ID the consumption belongs to, if any.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage orderId(String orderId) {
            this.orderId = Optional.ofNullable(orderId);
            return this;
        }

        /**
         * <p>Paid order display ID the consumption belongs to, if any.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "orderId", nulls = Nulls.SKIP)
        public _FinalStage orderId(Optional<String> orderId) {
            this.orderId = orderId;
            return this;
        }

        @java.lang.Override
        public _FinalStage creditCurrency(Nullable<PendingCreditConsumptionCurrency> creditCurrency) {
            if (creditCurrency.isNull()) {
                this.creditCurrency = null;
            } else if (creditCurrency.isEmpty()) {
                this.creditCurrency = Optional.empty();
            } else {
                this.creditCurrency = Optional.of(creditCurrency.get());
            }
            return this;
        }

        @java.lang.Override
        public _FinalStage creditCurrency(PendingCreditConsumptionCurrency creditCurrency) {
            this.creditCurrency = Optional.ofNullable(creditCurrency);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "creditCurrency", nulls = Nulls.SKIP)
        public _FinalStage creditCurrency(Optional<PendingCreditConsumptionCurrency> creditCurrency) {
            this.creditCurrency = creditCurrency;
            return this;
        }

        @java.lang.Override
        public PendingCreditConsumption build() {
            return new PendingCreditConsumption(
                    credits,
                    creditsDecimal,
                    creditCurrency,
                    orderId,
                    eventName,
                    occurredAt,
                    recordedAt,
                    additionalProperties);
        }
    }
}
