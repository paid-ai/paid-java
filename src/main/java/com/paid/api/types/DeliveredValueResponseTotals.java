/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = DeliveredValueResponseTotals.Builder.class)
public final class DeliveredValueResponseTotals {
    private final double monetaryTotal;

    private final double signalCount;

    private final Map<String, Object> additionalProperties;

    private DeliveredValueResponseTotals(
            double monetaryTotal, double signalCount, Map<String, Object> additionalProperties) {
        this.monetaryTotal = monetaryTotal;
        this.signalCount = signalCount;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Money delivered over the period, in decimal units of the response currency, not cents. Adds up the signals array exactly, so the breakdown always reconciles with this figure. That includes value whose metric the value model no longer defines, which is why this can exceed the sum of valueMetrics. Signals the value model no longer lists are outside the report altogether and appear in neither.
     */
    @JsonProperty("monetaryTotal")
    public double getMonetaryTotal() {
        return monetaryTotal;
    }

    /**
     * @return Total signal occurrences, counted the way a value receipt's footer counts them: events that delivered value, plus display-only events that fired. Quantity-weighted, so fractional where the signals reported fractional quantities.
     */
    @JsonProperty("signalCount")
    public double getSignalCount() {
        return signalCount;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof DeliveredValueResponseTotals && equalTo((DeliveredValueResponseTotals) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(DeliveredValueResponseTotals other) {
        return monetaryTotal == other.monetaryTotal && signalCount == other.signalCount;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.monetaryTotal, this.signalCount);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static MonetaryTotalStage builder() {
        return new Builder();
    }

    public interface MonetaryTotalStage {
        /**
         * <p>Money delivered over the period, in decimal units of the response currency, not cents. Adds up the signals array exactly, so the breakdown always reconciles with this figure. That includes value whose metric the value model no longer defines, which is why this can exceed the sum of valueMetrics. Signals the value model no longer lists are outside the report altogether and appear in neither.</p>
         */
        SignalCountStage monetaryTotal(double monetaryTotal);

        Builder from(DeliveredValueResponseTotals other);
    }

    public interface SignalCountStage {
        /**
         * <p>Total signal occurrences, counted the way a value receipt's footer counts them: events that delivered value, plus display-only events that fired. Quantity-weighted, so fractional where the signals reported fractional quantities.</p>
         */
        _FinalStage signalCount(double signalCount);
    }

    public interface _FinalStage {
        DeliveredValueResponseTotals build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements MonetaryTotalStage, SignalCountStage, _FinalStage {
        private double monetaryTotal;

        private double signalCount;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(DeliveredValueResponseTotals other) {
            monetaryTotal(other.getMonetaryTotal());
            signalCount(other.getSignalCount());
            return this;
        }

        /**
         * <p>Money delivered over the period, in decimal units of the response currency, not cents. Adds up the signals array exactly, so the breakdown always reconciles with this figure. That includes value whose metric the value model no longer defines, which is why this can exceed the sum of valueMetrics. Signals the value model no longer lists are outside the report altogether and appear in neither.</p>
         * <p>Money delivered over the period, in decimal units of the response currency, not cents. Adds up the signals array exactly, so the breakdown always reconciles with this figure. That includes value whose metric the value model no longer defines, which is why this can exceed the sum of valueMetrics. Signals the value model no longer lists are outside the report altogether and appear in neither.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("monetaryTotal")
        public SignalCountStage monetaryTotal(double monetaryTotal) {
            this.monetaryTotal = monetaryTotal;
            return this;
        }

        /**
         * <p>Total signal occurrences, counted the way a value receipt's footer counts them: events that delivered value, plus display-only events that fired. Quantity-weighted, so fractional where the signals reported fractional quantities.</p>
         * <p>Total signal occurrences, counted the way a value receipt's footer counts them: events that delivered value, plus display-only events that fired. Quantity-weighted, so fractional where the signals reported fractional quantities.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("signalCount")
        public _FinalStage signalCount(double signalCount) {
            this.signalCount = signalCount;
            return this;
        }

        @java.lang.Override
        public DeliveredValueResponseTotals build() {
            return new DeliveredValueResponseTotals(monetaryTotal, signalCount, additionalProperties);
        }
    }
}
