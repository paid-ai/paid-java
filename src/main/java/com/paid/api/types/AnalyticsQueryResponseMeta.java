/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = AnalyticsQueryResponseMeta.Builder.class)
public final class AnalyticsQueryResponseMeta {
    private final int rowCount;

    private final double elapsedMs;

    private final boolean truncated;

    private final Map<String, Object> additionalProperties;

    private AnalyticsQueryResponseMeta(
            int rowCount, double elapsedMs, boolean truncated, Map<String, Object> additionalProperties) {
        this.rowCount = rowCount;
        this.elapsedMs = elapsedMs;
        this.truncated = truncated;
        this.additionalProperties = additionalProperties;
    }

    @JsonProperty("rowCount")
    public int getRowCount() {
        return rowCount;
    }

    @JsonProperty("elapsedMs")
    public double getElapsedMs() {
        return elapsedMs;
    }

    /**
     * @return True when the result hit the row cap and was truncated. Narrow the query (shorter date range, LIMIT, coarser grouping) to see the rest.
     */
    @JsonProperty("truncated")
    public boolean getTruncated() {
        return truncated;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AnalyticsQueryResponseMeta && equalTo((AnalyticsQueryResponseMeta) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(AnalyticsQueryResponseMeta other) {
        return rowCount == other.rowCount && elapsedMs == other.elapsedMs && truncated == other.truncated;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.rowCount, this.elapsedMs, this.truncated);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static RowCountStage builder() {
        return new Builder();
    }

    public interface RowCountStage {
        ElapsedMsStage rowCount(int rowCount);

        Builder from(AnalyticsQueryResponseMeta other);
    }

    public interface ElapsedMsStage {
        TruncatedStage elapsedMs(double elapsedMs);
    }

    public interface TruncatedStage {
        /**
         * <p>True when the result hit the row cap and was truncated. Narrow the query (shorter date range, LIMIT, coarser grouping) to see the rest.</p>
         */
        _FinalStage truncated(boolean truncated);
    }

    public interface _FinalStage {
        AnalyticsQueryResponseMeta build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements RowCountStage, ElapsedMsStage, TruncatedStage, _FinalStage {
        private int rowCount;

        private double elapsedMs;

        private boolean truncated;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(AnalyticsQueryResponseMeta other) {
            rowCount(other.getRowCount());
            elapsedMs(other.getElapsedMs());
            truncated(other.getTruncated());
            return this;
        }

        @java.lang.Override
        @JsonSetter("rowCount")
        public ElapsedMsStage rowCount(int rowCount) {
            this.rowCount = rowCount;
            return this;
        }

        @java.lang.Override
        @JsonSetter("elapsedMs")
        public TruncatedStage elapsedMs(double elapsedMs) {
            this.elapsedMs = elapsedMs;
            return this;
        }

        /**
         * <p>True when the result hit the row cap and was truncated. Narrow the query (shorter date range, LIMIT, coarser grouping) to see the rest.</p>
         * <p>True when the result hit the row cap and was truncated. Narrow the query (shorter date range, LIMIT, coarser grouping) to see the rest.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("truncated")
        public _FinalStage truncated(boolean truncated) {
            this.truncated = truncated;
            return this;
        }

        @java.lang.Override
        public AnalyticsQueryResponseMeta build() {
            return new AnalyticsQueryResponseMeta(rowCount, elapsedMs, truncated, additionalProperties);
        }
    }
}
