/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CustomViewQuery.Builder.class)
public final class CustomViewQuery {
    private final String id;

    private final String sql;

    private final Map<String, Object> additionalProperties;

    private CustomViewQuery(String id, String sql, Map<String, Object> additionalProperties) {
        this.id = id;
        this.sql = sql;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Stable identifier for this query's result set; the render bundle reads <code>data[&lt;id&gt;]</code>.
     */
    @JsonProperty("id")
    public String getId() {
        return id;
    }

    /**
     * @return A single ClickHouse SELECT (or WITH … SELECT). Do NOT add a customer filter — the database scopes every query to the viewing customer at run time. Return clearly-named columns; bake any date range into WHERE.
     */
    @JsonProperty("sql")
    public String getSql() {
        return sql;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CustomViewQuery && equalTo((CustomViewQuery) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CustomViewQuery other) {
        return id.equals(other.id) && sql.equals(other.sql);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.id, this.sql);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static IdStage builder() {
        return new Builder();
    }

    public interface IdStage {
        /**
         * <p>Stable identifier for this query's result set; the render bundle reads <code>data[&lt;id&gt;]</code>.</p>
         */
        SqlStage id(@NotNull String id);

        Builder from(CustomViewQuery other);
    }

    public interface SqlStage {
        /**
         * <p>A single ClickHouse SELECT (or WITH … SELECT). Do NOT add a customer filter — the database scopes every query to the viewing customer at run time. Return clearly-named columns; bake any date range into WHERE.</p>
         */
        _FinalStage sql(@NotNull String sql);
    }

    public interface _FinalStage {
        CustomViewQuery build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements IdStage, SqlStage, _FinalStage {
        private String id;

        private String sql;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CustomViewQuery other) {
            id(other.getId());
            sql(other.getSql());
            return this;
        }

        /**
         * <p>Stable identifier for this query's result set; the render bundle reads <code>data[&lt;id&gt;]</code>.</p>
         * <p>Stable identifier for this query's result set; the render bundle reads <code>data[&lt;id&gt;]</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("id")
        public SqlStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        /**
         * <p>A single ClickHouse SELECT (or WITH … SELECT). Do NOT add a customer filter — the database scopes every query to the viewing customer at run time. Return clearly-named columns; bake any date range into WHERE.</p>
         * <p>A single ClickHouse SELECT (or WITH … SELECT). Do NOT add a customer filter — the database scopes every query to the viewing customer at run time. Return clearly-named columns; bake any date range into WHERE.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("sql")
        public _FinalStage sql(@NotNull String sql) {
            this.sql = Objects.requireNonNull(sql, "sql must not be null");
            return this;
        }

        @java.lang.Override
        public CustomViewQuery build() {
            return new CustomViewQuery(id, sql, additionalProperties);
        }
    }
}
