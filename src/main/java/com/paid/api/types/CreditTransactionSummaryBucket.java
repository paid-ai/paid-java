/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.Nullable;
import com.paid.api.core.NullableNonemptyFilter;
import com.paid.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CreditTransactionSummaryBucket.Builder.class)
public final class CreditTransactionSummaryBucket {
    private final OffsetDateTime periodStart;

    private final OffsetDateTime periodEnd;

    private final Optional<String> groupKey;

    private final CreditTransactionSummaryBucketCreditCurrency creditCurrency;

    private final String credits;

    private final int transactionCount;

    private final Map<String, Object> additionalProperties;

    private CreditTransactionSummaryBucket(
            OffsetDateTime periodStart,
            OffsetDateTime periodEnd,
            Optional<String> groupKey,
            CreditTransactionSummaryBucketCreditCurrency creditCurrency,
            String credits,
            int transactionCount,
            Map<String, Object> additionalProperties) {
        this.periodStart = periodStart;
        this.periodEnd = periodEnd;
        this.groupKey = groupKey;
        this.creditCurrency = creditCurrency;
        this.credits = credits;
        this.transactionCount = transactionCount;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Start of the period bucket (inclusive), UTC. For the first bucket this is <code>createdAtFrom</code> when the window starts inside the period.
     */
    @JsonProperty("periodStart")
    public OffsetDateTime getPeriodStart() {
        return periodStart;
    }

    /**
     * @return Last instant of the period bucket (inclusive), UTC; for the last bucket this is <code>createdAtTo</code> (or the request time) when the window ends inside the period. To fetch exactly this bucket's transactions, call the transaction list with <code>createdAtFrom</code>/<code>createdAtTo</code> set to <code>periodStart</code>/<code>periodEnd</code>, <code>creditCurrencyKey</code> set to this bucket's currency key, <code>eventName</code> set to <code>groupKey</code>, and the same <code>type</code> and other filters. A null <code>groupKey</code> has no event to filter on; select those rows client-side by their null <code>eventName</code>.
     */
    @JsonProperty("periodEnd")
    public OffsetDateTime getPeriodEnd() {
        return periodEnd;
    }

    /**
     * @return Value of the <code>groupBy</code> dimension. For <code>eventName</code>, the usage event that produced the spends; null for transactions with no event (grants, invoice-void clawbacks, manual adjustments).
     */
    @JsonIgnore
    public Optional<String> getGroupKey() {
        if (groupKey == null) {
            return Optional.empty();
        }
        return groupKey;
    }

    /**
     * @return Credits are counted per currency and never summed across currencies.
     */
    @JsonProperty("creditCurrency")
    public CreditTransactionSummaryBucketCreditCurrency getCreditCurrency() {
        return creditCurrency;
    }

    /**
     * @return Net credits in this bucket as an exact decimal string, in the type's natural direction: for <code>spend</code>, the credits consumed (positive; negative only when the bucket holds solely reversals); for <code>grant</code> and <code>pendingGrant</code>, the credits granted. Exact at any magnitude and at the full 6 decimal places. Summing the transaction list's <code>amount</code> over the same filters and period gives the same value (negated for <code>spend</code>).
     */
    @JsonProperty("credits")
    public String getCredits() {
        return credits;
    }

    /**
     * @return Number of ledger transactions in this bucket.
     */
    @JsonProperty("transactionCount")
    public int getTransactionCount() {
        return transactionCount;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("groupKey")
    private Optional<String> _getGroupKey() {
        return groupKey;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreditTransactionSummaryBucket && equalTo((CreditTransactionSummaryBucket) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CreditTransactionSummaryBucket other) {
        return periodStart.equals(other.periodStart)
                && periodEnd.equals(other.periodEnd)
                && groupKey.equals(other.groupKey)
                && creditCurrency.equals(other.creditCurrency)
                && credits.equals(other.credits)
                && transactionCount == other.transactionCount;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.periodStart,
                this.periodEnd,
                this.groupKey,
                this.creditCurrency,
                this.credits,
                this.transactionCount);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static PeriodStartStage builder() {
        return new Builder();
    }

    public interface PeriodStartStage {
        /**
         * <p>Start of the period bucket (inclusive), UTC. For the first bucket this is <code>createdAtFrom</code> when the window starts inside the period.</p>
         */
        PeriodEndStage periodStart(@NotNull OffsetDateTime periodStart);

        Builder from(CreditTransactionSummaryBucket other);
    }

    public interface PeriodEndStage {
        /**
         * <p>Last instant of the period bucket (inclusive), UTC; for the last bucket this is <code>createdAtTo</code> (or the request time) when the window ends inside the period. To fetch exactly this bucket's transactions, call the transaction list with <code>createdAtFrom</code>/<code>createdAtTo</code> set to <code>periodStart</code>/<code>periodEnd</code>, <code>creditCurrencyKey</code> set to this bucket's currency key, <code>eventName</code> set to <code>groupKey</code>, and the same <code>type</code> and other filters. A null <code>groupKey</code> has no event to filter on; select those rows client-side by their null <code>eventName</code>.</p>
         */
        CreditCurrencyStage periodEnd(@NotNull OffsetDateTime periodEnd);
    }

    public interface CreditCurrencyStage {
        /**
         * <p>Credits are counted per currency and never summed across currencies.</p>
         */
        CreditsStage creditCurrency(@NotNull CreditTransactionSummaryBucketCreditCurrency creditCurrency);
    }

    public interface CreditsStage {
        /**
         * <p>Net credits in this bucket as an exact decimal string, in the type's natural direction: for <code>spend</code>, the credits consumed (positive; negative only when the bucket holds solely reversals); for <code>grant</code> and <code>pendingGrant</code>, the credits granted. Exact at any magnitude and at the full 6 decimal places. Summing the transaction list's <code>amount</code> over the same filters and period gives the same value (negated for <code>spend</code>).</p>
         */
        TransactionCountStage credits(@NotNull String credits);
    }

    public interface TransactionCountStage {
        /**
         * <p>Number of ledger transactions in this bucket.</p>
         */
        _FinalStage transactionCount(int transactionCount);
    }

    public interface _FinalStage {
        CreditTransactionSummaryBucket build();

        /**
         * <p>Value of the <code>groupBy</code> dimension. For <code>eventName</code>, the usage event that produced the spends; null for transactions with no event (grants, invoice-void clawbacks, manual adjustments).</p>
         */
        _FinalStage groupKey(Optional<String> groupKey);

        _FinalStage groupKey(String groupKey);

        _FinalStage groupKey(Nullable<String> groupKey);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements PeriodStartStage,
                    PeriodEndStage,
                    CreditCurrencyStage,
                    CreditsStage,
                    TransactionCountStage,
                    _FinalStage {
        private OffsetDateTime periodStart;

        private OffsetDateTime periodEnd;

        private CreditTransactionSummaryBucketCreditCurrency creditCurrency;

        private String credits;

        private int transactionCount;

        private Optional<String> groupKey = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CreditTransactionSummaryBucket other) {
            periodStart(other.getPeriodStart());
            periodEnd(other.getPeriodEnd());
            groupKey(other.getGroupKey());
            creditCurrency(other.getCreditCurrency());
            credits(other.getCredits());
            transactionCount(other.getTransactionCount());
            return this;
        }

        /**
         * <p>Start of the period bucket (inclusive), UTC. For the first bucket this is <code>createdAtFrom</code> when the window starts inside the period.</p>
         * <p>Start of the period bucket (inclusive), UTC. For the first bucket this is <code>createdAtFrom</code> when the window starts inside the period.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("periodStart")
        public PeriodEndStage periodStart(@NotNull OffsetDateTime periodStart) {
            this.periodStart = Objects.requireNonNull(periodStart, "periodStart must not be null");
            return this;
        }

        /**
         * <p>Last instant of the period bucket (inclusive), UTC; for the last bucket this is <code>createdAtTo</code> (or the request time) when the window ends inside the period. To fetch exactly this bucket's transactions, call the transaction list with <code>createdAtFrom</code>/<code>createdAtTo</code> set to <code>periodStart</code>/<code>periodEnd</code>, <code>creditCurrencyKey</code> set to this bucket's currency key, <code>eventName</code> set to <code>groupKey</code>, and the same <code>type</code> and other filters. A null <code>groupKey</code> has no event to filter on; select those rows client-side by their null <code>eventName</code>.</p>
         * <p>Last instant of the period bucket (inclusive), UTC; for the last bucket this is <code>createdAtTo</code> (or the request time) when the window ends inside the period. To fetch exactly this bucket's transactions, call the transaction list with <code>createdAtFrom</code>/<code>createdAtTo</code> set to <code>periodStart</code>/<code>periodEnd</code>, <code>creditCurrencyKey</code> set to this bucket's currency key, <code>eventName</code> set to <code>groupKey</code>, and the same <code>type</code> and other filters. A null <code>groupKey</code> has no event to filter on; select those rows client-side by their null <code>eventName</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("periodEnd")
        public CreditCurrencyStage periodEnd(@NotNull OffsetDateTime periodEnd) {
            this.periodEnd = Objects.requireNonNull(periodEnd, "periodEnd must not be null");
            return this;
        }

        /**
         * <p>Credits are counted per currency and never summed across currencies.</p>
         * <p>Credits are counted per currency and never summed across currencies.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("creditCurrency")
        public CreditsStage creditCurrency(@NotNull CreditTransactionSummaryBucketCreditCurrency creditCurrency) {
            this.creditCurrency = Objects.requireNonNull(creditCurrency, "creditCurrency must not be null");
            return this;
        }

        /**
         * <p>Net credits in this bucket as an exact decimal string, in the type's natural direction: for <code>spend</code>, the credits consumed (positive; negative only when the bucket holds solely reversals); for <code>grant</code> and <code>pendingGrant</code>, the credits granted. Exact at any magnitude and at the full 6 decimal places. Summing the transaction list's <code>amount</code> over the same filters and period gives the same value (negated for <code>spend</code>).</p>
         * <p>Net credits in this bucket as an exact decimal string, in the type's natural direction: for <code>spend</code>, the credits consumed (positive; negative only when the bucket holds solely reversals); for <code>grant</code> and <code>pendingGrant</code>, the credits granted. Exact at any magnitude and at the full 6 decimal places. Summing the transaction list's <code>amount</code> over the same filters and period gives the same value (negated for <code>spend</code>).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("credits")
        public TransactionCountStage credits(@NotNull String credits) {
            this.credits = Objects.requireNonNull(credits, "credits must not be null");
            return this;
        }

        /**
         * <p>Number of ledger transactions in this bucket.</p>
         * <p>Number of ledger transactions in this bucket.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("transactionCount")
        public _FinalStage transactionCount(int transactionCount) {
            this.transactionCount = transactionCount;
            return this;
        }

        /**
         * <p>Value of the <code>groupBy</code> dimension. For <code>eventName</code>, the usage event that produced the spends; null for transactions with no event (grants, invoice-void clawbacks, manual adjustments).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage groupKey(Nullable<String> groupKey) {
            if (groupKey.isNull()) {
                this.groupKey = null;
            } else if (groupKey.isEmpty()) {
                this.groupKey = Optional.empty();
            } else {
                this.groupKey = Optional.of(groupKey.get());
            }
            return this;
        }

        /**
         * <p>Value of the <code>groupBy</code> dimension. For <code>eventName</code>, the usage event that produced the spends; null for transactions with no event (grants, invoice-void clawbacks, manual adjustments).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage groupKey(String groupKey) {
            this.groupKey = Optional.ofNullable(groupKey);
            return this;
        }

        /**
         * <p>Value of the <code>groupBy</code> dimension. For <code>eventName</code>, the usage event that produced the spends; null for transactions with no event (grants, invoice-void clawbacks, manual adjustments).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "groupKey", nulls = Nulls.SKIP)
        public _FinalStage groupKey(Optional<String> groupKey) {
            this.groupKey = groupKey;
            return this;
        }

        @java.lang.Override
        public CreditTransactionSummaryBucket build() {
            return new CreditTransactionSummaryBucket(
                    periodStart, periodEnd, groupKey, creditCurrency, credits, transactionCount, additionalProperties);
        }
    }
}
