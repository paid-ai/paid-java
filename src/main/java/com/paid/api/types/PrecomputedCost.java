/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.paid.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.paid.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = PrecomputedCost.Builder.class)
public final class PrecomputedCost {
    private final Optional<OffsetDateTime> timestamp;

    private final CustomerAttribution customer;

    private final Optional<ProductAttribution> product;

    private final Optional<Map<String, Object>> metadata;

    private final double amount;

    private final String currency;

    private final Optional<String> vendor;

    private final Optional<String> model;

    private final Map<String, Object> additionalProperties;

    private PrecomputedCost(
            Optional<OffsetDateTime> timestamp,
            CustomerAttribution customer,
            Optional<ProductAttribution> product,
            Optional<Map<String, Object>> metadata,
            double amount,
            String currency,
            Optional<String> vendor,
            Optional<String> model,
            Map<String, Object> additionalProperties) {
        this.timestamp = timestamp;
        this.customer = customer;
        this.product = product;
        this.metadata = metadata;
        this.amount = amount;
        this.currency = currency;
        this.vendor = vendor;
        this.model = model;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Record time in RFC3339 format with timezone. Defaults to server receive time.
     */
    @JsonProperty("timestamp")
    public Optional<OffsetDateTime> getTimestamp() {
        return timestamp;
    }

    @JsonProperty("customer")
    public CustomerAttribution getCustomer() {
        return customer;
    }

    @JsonProperty("product")
    public Optional<ProductAttribution> getProduct() {
        return product;
    }

    /**
     * @return Free-form metadata for filtering and analytics.
     */
    @JsonProperty("metadata")
    public Optional<Map<String, Object>> getMetadata() {
        return metadata;
    }

    /**
     * @return Cost amount in major units of <code>currency</code> (e.g. dollars).
     */
    @JsonProperty("amount")
    public double getAmount() {
        return amount;
    }

    /**
     * @return ISO 4217 currency code, e.g. &quot;USD&quot;.
     */
    @JsonProperty("currency")
    public String getCurrency() {
        return currency;
    }

    /**
     * @return Optional vendor tag for analytics (e.g. &quot;openai&quot;, &quot;anthropic&quot;). Not used for pricing in this mode.
     */
    @JsonProperty("vendor")
    public Optional<String> getVendor() {
        return vendor;
    }

    /**
     * @return Optional model tag for analytics (e.g. &quot;gpt-4o-2024-08-06&quot;).
     */
    @JsonProperty("model")
    public Optional<String> getModel() {
        return model;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PrecomputedCost && equalTo((PrecomputedCost) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(PrecomputedCost other) {
        return timestamp.equals(other.timestamp)
                && customer.equals(other.customer)
                && product.equals(other.product)
                && metadata.equals(other.metadata)
                && amount == other.amount
                && currency.equals(other.currency)
                && vendor.equals(other.vendor)
                && model.equals(other.model);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.timestamp,
                this.customer,
                this.product,
                this.metadata,
                this.amount,
                this.currency,
                this.vendor,
                this.model);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static CustomerStage builder() {
        return new Builder();
    }

    public interface CustomerStage {
        AmountStage customer(@NotNull CustomerAttribution customer);

        Builder from(PrecomputedCost other);
    }

    public interface AmountStage {
        /**
         * <p>Cost amount in major units of <code>currency</code> (e.g. dollars).</p>
         */
        CurrencyStage amount(double amount);
    }

    public interface CurrencyStage {
        /**
         * <p>ISO 4217 currency code, e.g. &quot;USD&quot;.</p>
         */
        _FinalStage currency(@NotNull String currency);
    }

    public interface _FinalStage {
        PrecomputedCost build();

        /**
         * <p>Record time in RFC3339 format with timezone. Defaults to server receive time.</p>
         */
        _FinalStage timestamp(Optional<OffsetDateTime> timestamp);

        _FinalStage timestamp(OffsetDateTime timestamp);

        _FinalStage product(Optional<ProductAttribution> product);

        _FinalStage product(ProductAttribution product);

        /**
         * <p>Free-form metadata for filtering and analytics.</p>
         */
        _FinalStage metadata(Optional<Map<String, Object>> metadata);

        _FinalStage metadata(Map<String, Object> metadata);

        /**
         * <p>Optional vendor tag for analytics (e.g. &quot;openai&quot;, &quot;anthropic&quot;). Not used for pricing in this mode.</p>
         */
        _FinalStage vendor(Optional<String> vendor);

        _FinalStage vendor(String vendor);

        /**
         * <p>Optional model tag for analytics (e.g. &quot;gpt-4o-2024-08-06&quot;).</p>
         */
        _FinalStage model(Optional<String> model);

        _FinalStage model(String model);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements CustomerStage, AmountStage, CurrencyStage, _FinalStage {
        private CustomerAttribution customer;

        private double amount;

        private String currency;

        private Optional<String> model = Optional.empty();

        private Optional<String> vendor = Optional.empty();

        private Optional<Map<String, Object>> metadata = Optional.empty();

        private Optional<ProductAttribution> product = Optional.empty();

        private Optional<OffsetDateTime> timestamp = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(PrecomputedCost other) {
            timestamp(other.getTimestamp());
            customer(other.getCustomer());
            product(other.getProduct());
            metadata(other.getMetadata());
            amount(other.getAmount());
            currency(other.getCurrency());
            vendor(other.getVendor());
            model(other.getModel());
            return this;
        }

        @java.lang.Override
        @JsonSetter("customer")
        public AmountStage customer(@NotNull CustomerAttribution customer) {
            this.customer = Objects.requireNonNull(customer, "customer must not be null");
            return this;
        }

        /**
         * <p>Cost amount in major units of <code>currency</code> (e.g. dollars).</p>
         * <p>Cost amount in major units of <code>currency</code> (e.g. dollars).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("amount")
        public CurrencyStage amount(double amount) {
            this.amount = amount;
            return this;
        }

        /**
         * <p>ISO 4217 currency code, e.g. &quot;USD&quot;.</p>
         * <p>ISO 4217 currency code, e.g. &quot;USD&quot;.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("currency")
        public _FinalStage currency(@NotNull String currency) {
            this.currency = Objects.requireNonNull(currency, "currency must not be null");
            return this;
        }

        /**
         * <p>Optional model tag for analytics (e.g. &quot;gpt-4o-2024-08-06&quot;).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage model(String model) {
            this.model = Optional.ofNullable(model);
            return this;
        }

        /**
         * <p>Optional model tag for analytics (e.g. &quot;gpt-4o-2024-08-06&quot;).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "model", nulls = Nulls.SKIP)
        public _FinalStage model(Optional<String> model) {
            this.model = model;
            return this;
        }

        /**
         * <p>Optional vendor tag for analytics (e.g. &quot;openai&quot;, &quot;anthropic&quot;). Not used for pricing in this mode.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage vendor(String vendor) {
            this.vendor = Optional.ofNullable(vendor);
            return this;
        }

        /**
         * <p>Optional vendor tag for analytics (e.g. &quot;openai&quot;, &quot;anthropic&quot;). Not used for pricing in this mode.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "vendor", nulls = Nulls.SKIP)
        public _FinalStage vendor(Optional<String> vendor) {
            this.vendor = vendor;
            return this;
        }

        /**
         * <p>Free-form metadata for filtering and analytics.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage metadata(Map<String, Object> metadata) {
            this.metadata = Optional.ofNullable(metadata);
            return this;
        }

        /**
         * <p>Free-form metadata for filtering and analytics.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "metadata", nulls = Nulls.SKIP)
        public _FinalStage metadata(Optional<Map<String, Object>> metadata) {
            this.metadata = metadata;
            return this;
        }

        @java.lang.Override
        public _FinalStage product(ProductAttribution product) {
            this.product = Optional.ofNullable(product);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "product", nulls = Nulls.SKIP)
        public _FinalStage product(Optional<ProductAttribution> product) {
            this.product = product;
            return this;
        }

        /**
         * <p>Record time in RFC3339 format with timezone. Defaults to server receive time.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage timestamp(OffsetDateTime timestamp) {
            this.timestamp = Optional.ofNullable(timestamp);
            return this;
        }

        /**
         * <p>Record time in RFC3339 format with timezone. Defaults to server receive time.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "timestamp", nulls = Nulls.SKIP)
        public _FinalStage timestamp(Optional<OffsetDateTime> timestamp) {
            this.timestamp = timestamp;
            return this;
        }

        @java.lang.Override
        public PrecomputedCost build() {
            return new PrecomputedCost(
                    timestamp, customer, product, metadata, amount, currency, vendor, model, additionalProperties);
        }
    }
}
